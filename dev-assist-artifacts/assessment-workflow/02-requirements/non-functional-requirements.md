# CAP-02 Non-Functional Requirements (Quality Attributes)

**Capability**: CAP-02 — academically-sound, repeatable, reportable assessment **workflow** that runs the **existing** `pii-rate-elo` tournament against PII-Anon **v2.0.0 / 575,604 records / CC0 / `annotations`** over a **powered, lattice-stratified sample (default)** / **full corpus (opt-in, citable)** / **smoke (fast)** — with statistical/epistemic observability + reporting at every spine stage (`load → sample → run → score → rate → report`).
**Stage**: assessment-workflow / 02-Requirements · R4 (NFR authoring)
**Date**: 2026-06-01
**Vocabulary remap**: NFR = **Quality Attribute**; FR = Benchmark/Assessment Capability; UC = Evaluation Scenario; DC = Benchmark Component; Persona = assessment consumer.
**ID discipline**: global numbering **continued, never reused**. Cycle-1 ended at **NFR-018**; CAP-02 runs **NFR-019 … NFR-043** (25 new Quality Attributes). Inherited cycle-1 NFR-001…NFR-018 remain binding (the no-regression guardrails below pin the load-bearing ones: NFR-005, NFR-010, NFR-013, NFR-018).
**provisional_status**: AGENT_SIMULATED — thresholds are agent-authored and (where flagged) carry `real_user_needed: true` for Pass-2; the **code/version/line evidence is direct file read at HEAD on 2026-06-01** (paths cited inline). Agent-simulated research is NOT a substitute for real users.

> **Architecture (locked).** eval-data **OWNS** sampling + observability + reporting; `pii-rate-elo` **CONSUMES** (extend, do NOT rebuild the engine/metrics). Every NFR below is authored to that boundary: the *machinery* (Glicko engine, convergence checker, span-metric bridge) is consumed; the *statistical/epistemic guarantees* (CIs, paired tests, power verdicts, provenance, caveats) are contributed by eval-data and are what these NFRs gate.

> **Each NFR carries:** `id` · `title` · a **QUANTIFIED threshold (number + unit) OR a boolean-auditable criterion** · a **measurement method** · the **axiom / precondition** it serves. "Boolean-auditable" = a single predicate a CI job (or a named reviewer) can assert true/false. Where a number is grounded in code, the file + symbol is cited so Design/Dev can re-confirm.

---

## Proposed axiom — AX-pii-anon-005 (recorded here for PO confirmation)

> **AX-pii-anon-005 (Pre-registered / reproducible assessment).** **No published metric ships without all of:** (1) a **confidence interval** with the interval-selection method named; (2) a **paired test** backing every system-vs-system claim, under an **explicitly declared multiplicity family** — *two* inference families: **exploratory** per-cell uncorrected (labelled, never a headline) vs **confirmatory** pairwise **Holm-Bonferroni with the family size stated** (declaration is in the **axiom/report text, not only in code** — "reviewers will ask"; INT-01/INT-02); (3) a **convergence statement** reported as **achieved max-RD ± 2RD (95%) with the rounds/stopping rule stated**, never an extrapolated round count (G4; INT-02); (4) **provenance** sufficient to reproduce the run from the manifest (`{dataset_version, record_count, schema_fingerprint, seed, stage, code_commit, toolchain}` + file-level provenance); (5) a **non-strippable synthetic-only / anti-anonymity caveat** on every metric (AX-001/003); and — for leaderboard/published entries — (6) **contamination disclosure**: a per-system `contamination_status` with **`unknown` rejected/flagged**, plus a **signed attestation that held-out labels were not exposed** (INT-03's 6th element).
>
> **Enforcement scope is run-type-bound** (the §4 universality tension, resolved): the **full** AX-005 bar binds the `leaderboard-submission` / `filing-grade` run-types; `smoke` / `dev` runs **suppress CI + p-value emission entirely** (UC-20) — there is no un-rigorous number to mis-cite because there is *no number* — which satisfies both "the exploratory run is where people get sloppy" (INT-02) and "universal enforcement makes teams avoid the formal mode" (INT-05).
>
> **Status:** PROPOSED — confirmed as "the right bar" by all interviewees who reached probe #17, with the two sharpenings above (multiplicity-family-in-text; 6th contamination element) and run-type-scoped enforcement. The NFRs in this document are the testable operationalization of this axiom. **Inherited binding axioms unchanged:** AX-001 (synthetic-only) · AX-002 (deterministic/seeded/byte-reproducible) · AX-003 (stated power — n + CI) · AX-004 (anon vs pseudo, separate metric families).

---

## A. Statistics-core integrity (the SHOWSTOPPER precondition P1)

- **NFR-019 — Run path uses ONLY the audited eval-data statistics core; `significance.py` quarantined.** *Threshold (boolean-auditable):* **zero** statistical quantities on the assessment run path are produced by `pii-rate-elo/analysis/significance.py::SignificanceTester`; every CI and every paired test is computed by eval-data `stats/intervals.py` (`wilson_interval` / `clopper_pearson_interval`) and `stats/paired.py` (`mcnemar_exact` / `mcnemar_chi2` / `paired_bootstrap_recall_delta`). The assessment preset sets `analysis.run_significance_tests: false` (it is read at `cli.py:240`, which instantiates `SignificanceTester()` at `cli.py:242`); a CI guard asserts the assessment code path never imports/instantiates `SignificanceTester`. *Measure:* **CI gate (blocking):** import-graph / call-graph static check on the assessment entrypoint asserting `SignificanceTester` is **unreachable** AND a config assertion `run_significance_tests == False` — these two CI predicates ARE the gate (green requires no human). Reviewer audit is a **secondary, non-gating** corroboration only. *Serves:* **P1 (SHOWSTOPPER)**; AX-pii-anon-005 (CI + paired test).

- **NFR-020 — Zero approximated statistics on the run path (fabrication ban).** *Threshold (boolean-auditable):* **zero** occurrences on the run path of the fabricated forms verified at HEAD — `se = metric_value*(1-metric_value)/100` + `np.random.normal(...)` (`significance.py:205-206`), `n_approx = 100` / `pooled_sd = 0.1` / `z_stat = difference*sqrt(n_approx)/0.05` (`significance.py:271-281`) — i.e. **no** standard error from a hardcoded constant instead of `n`, **no** Gaussian noise as a substitute for resampling, **no** McNemar that never sees a discordant-pair `(b, c)` count. Every paired test on the run path consumes **real discordant-pair counts**; every bootstrap **resamples pair indices**. *Measure:* **CI gate (blocking):** grep-based forbidden-pattern lint over the **declared run-path module set** against a **committed regex list** — the three verified fabrication signatures `{ *(1-*)/100 , np.random.normal , (n_approx | pooled_sd\s*=\s*0\.1 | sqrt\(n_approx\)/0\.05) }` — pass condition = **zero matches**. Reviewer diff of the two implementations is a **secondary, non-gating** corroboration. *Serves:* **P1**; AX-pii-anon-005.

- **NFR-021 — Seeded LOCAL-RNG determinism for all resampling (no global RNG on the run path).** *Threshold (boolean-auditable):* every bootstrap / resample on the run path drives a **local `random.Random(seed)`** instance and **never** the module-global RNG or `numpy` global RNG; two runs with the same seed are **byte-identical** in their resampled outputs regardless of ambient `random.seed(...)`. (`stats/paired.py::paired_bootstrap_recall_delta` already owns a local `random.Random(seed)` at L140; the fabricated `significance.py` path uses `np.random` global RNG — banned by NFR-019/020.) *Measure:* same-seed re-run byte-equality test for every resampled statistic + static check for `np.random.` / bare `random.` global calls on the run path. *Serves:* **P1**; AX-002; AX-pii-anon-005.

- **NFR-022 — Integer-guarded interval inputs (no fractional Bernoulli counts).** *Threshold (boolean-auditable):* every CI on the run path is computed from **honest integer `(k, n)` Bernoulli counts** with `0 ≤ k ≤ n`; fractional/partial counts and `bool` inputs are **rejected with `TypeError`/`ValueError`** (the reidx-02 guard `stats/intervals.py::_require_int_counts`, L39-51, shared by `wilson_interval` and `clopper_pearson_interval`). No interval is ever derived from a smoothed/blended/partial count. *Measure:* the existing reidx-02 guard tests stay green + a run-path assertion that interval inputs are `int`. *Serves:* **P1**; AX-003.

## B. Confidence intervals on every metric (AX-003 / AX-005 element 1)

- **NFR-023 — CI on 100% of published metrics, with a deterministic interval-selection rule.** *Threshold:* **100%** of published metrics carry `{n, ci_low, ci_high, method}`; **zero** bare point estimates. The `method` is assigned by a **deterministic, pre-registered selection rule**: proportions → **Wilson** (default); **boundary** (point estimate at 0 or 1, i.e. `k ∈ {0, n}`) → **Clopper-Pearson** (this boundary clause is the load-bearing, statistically-mandated branch — it fixes Wilson's degenerate behaviour at `p̂ = 0|1`); small-n (`n < small_n_cutoff`, where **`small_n_cutoff` is a single integer RECORDED in the manifest + pre-registration**, committed default **`small_n_cutoff = 15`** — *R10 DIVERGED edit: lowered from 30 → 15 toward the Wilson-robust-to-n≈10 zone, since both the cited binomial-CI literature [Brown/Cai/DasGupta 2001; Wikipedia] AND both R10 personas agree the n-based Clopper-Pearson switch is over-conservative and that the literature endorses **no** specific integer; the value remains R10-DIRECTIONAL / `real_user_needed: true` pending a real ACL/PETS/de-id reviewer in Pass-2, never a silent constant*) → **Clopper-Pearson**; paired differences → **paired-bootstrap**. *Measure:* report linter asserts every metric row has a non-null CI with `method ∈ {wilson, clopper-pearson, paired-bootstrap}` whose value **equals the method the recorded `small_n_cutoff` prescribes for that row's `(k, n)` class** — a `method` inconsistent with the recorded cutoff fails (CI-blocking). *Serves:* AX-003; AX-pii-anon-005 (element 1); UC-17(a), UC-23(a). *(`real_user_needed: true` on the `small_n_cutoff` integer — the boundary clause and the determinism/manifest-recording contract are firm; only the n-switch integer awaits a real reviewer.)*

- **NFR-024 — CI scope is conditional-on-this-sample; corpus-draw variance declared.** *Threshold (boolean-auditable):* every reported CI is labelled **`scope: conditional-on-this-sample`**; corpus-draw (seed-to-seed) variance is **either** measured (**`len(seeds) ≥ MIN_SEEDS`, committed `MIN_SEEDS = 3`** — reconciled with FR-043's INT-02 3-seed floor; *R10 may relax to 2 with recorded rationale* — with **Kendall-τ rank stability** reported) **or** explicitly flagged `seed_variance_scope: single-seed` → carried to the report as **`rank-volatility: UNMEASURED`**. No CI is presented as if it captured resampling variance it did not measure. *Measure:* presence of `scope` on every CI + presence of either the `len(seeds) ≥ MIN_SEEDS (==3)` + Kendall-τ block or the single-seed flag, asserted by the report linter (CI-blocking). *Serves:* AX-003; AX-pii-anon-005; UC-17(e), UC-23(d).

- **NFR-025 — Census runs are descriptive (no CI) unless a super-population target is declared.** *Threshold (boolean-auditable):* a **full-corpus** run (UC-19) labels its metrics **exact-on-corpus / descriptive-census with NO CI by default**; CIs are retained **only** when the report **explicitly declares the inferential target is a super-population** (future inputs), not the fixed corpus. *Measure:* run-record `inferential_target ∈ {exact-on-corpus, super-population}` + linter (census + no declaration ⇒ CI fields must be absent). *Serves:* AX-003 (honest n-semantics); UC-19.

## C. Paired tests + multiplicity (AX-005 element 2)

- **NFR-026 — Paired test required for every system-vs-system claim.** *Threshold (boolean-auditable):* **every** confirmatory pairwise system claim carries a **McNemar verdict** (exact binomial / Edwards continuity-corrected χ², `stats/paired.py`) **AND** a **paired-bootstrap** recall-delta CI. A directional claim with no paired test is **rejected** (not merely warned). *Measure:* report linter — every pairwise row has a McNemar p-value + a paired-bootstrap delta CI (CI-blocking). *Serves:* AX-pii-anon-005 (element 2); UC-17(b), UC-23(b); closes the "delta within noise, no significance test" rejection vector (INT-01/02).

- **NFR-027 — Two declared inference families with Holm-Bonferroni and stated family size.** *Threshold (boolean-auditable):* the report declares **exactly two inference families** — **exploratory** (per-cell, uncorrected, **labelled as such and never a headline claim**) vs **confirmatory** (pairwise, **Holm-Bonferroni** corrected, with the **family size = #pairs × #metrics in the confirmatory set printed**). The family declaration is carried as a **structured `inference_families` block** (the report prose renders from it) present in **both** the report payload and the pre-reg payload. *Measure:* linter asserts the `inference_families` block carries exactly the two labels `{exploratory, confirmatory}`, every confirmatory claim carries a Holm-adjusted decision, and `confirmatory.family_size` is an integer **`== #pairs × #metrics in the confirmatory set`** (computed, not free-typed); a missing block in either payload, only-one-family, or `family_size ≠` the product fails; pre-reg↔report parity check (UC-18) (CI-blocking). *Serves:* AX-pii-anon-005 (element 2, sharpening a); UC-17(a), UC-23(b).

- **NFR-028 — Ranks gated by the paired-test verdict (ties greyed).** *Threshold (boolean-auditable):* in the leaderboard, any system pair whose difference is **not significant post-Holm-Bonferroni** is **grouped/greyed as a statistical tie**; **no rank out-runs its paired evidence**. *Measure:* report linter — for every adjacent rank pair, a significant Holm decision exists OR the pair is rendered as a tie (CI-blocking). *Serves:* AX-pii-anon-005 (element 2); UC-23(b); STATS-07.

## D. Convergence statement (AX-005 element 3)

- **NFR-029 — RD-convergence reported as achieved max-RD ± 2RD with the stopping rule stated.** *Threshold (boolean-auditable):* the report states the consumed `ConvergenceChecker` verdict as **`{is_converged, max_rd, rd_threshold}` PLUS the achieved max-RD with its ±2RD (95%) interval PLUS the stopping rule (max rounds / seed budget)**; convergence is **NEVER** expressed as an extrapolated round count (G4). A **NOT-converged** verdict **propagates as a blocking honesty flag into the report** (UC-23). *Measure:* linter asserts the convergence block has all four fields + the ±2RD interval + the stopping rule, and that a `NOT-converged` verdict appears on the headline; reuses `pii-rate-elo/tournament/convergence.py::ConvergenceChecker` (Glicko RD, default threshold 100). *Serves:* AX-pii-anon-005 (element 3); G4; UC-17(c), UC-23(d).

## E. Reproducibility — manifest, pre-registration, run-type (AX-002 / AX-005 elements 4 & enforcement scope)

- **NFR-030 — Byte-reproducibility of sample + run from the manifest (UNIVERSAL).** *Threshold (boolean-auditable):* re-running the sampler and the run from the recorded manifest reproduces the **sample manifest under canonical-form equality** (sorted keys + fixed float repr) **AND** reproduces every seeded artifact **byte-identically**, given the **same seed AND the same recorded environment/RNG fingerprint (RNG algorithm + seed-derivation, not just the seed integer) AND the same sampler version (commit/hash of `subsets/slices.py` + `stats/power.py` pinned in the manifest)**. This is the **universal** commitment (pre-registration is opt-in; manifest-reproducibility is not). *Measure:* CI re-run of sampler → canonical-form manifest diff empty; re-run of seeded run-record → byte diff empty (pattern grounded in `scripts/benchmark_throughput.py`, byte-reproducible under `SEED_BENCHMARK=20260531`). *Serves:* AX-002; AX-pii-anon-005 (element 4); UC-16 acceptance signal.

- **NFR-031 — Pre-registration completeness (opt-in rigor), commit-anchored + hash-chained.** *Threshold (boolean-auditable):* when pre-registration is enabled (run-type-scoped — see NFR-033), the pre-reg is bound to a **git commit SHA pushed to remote** and **chained into the first run-record**, is **immutable**, and its equality set covers **all** of: `{dataset_version, sample-manifest hash, seed, system list, metric set, rd_stopping_rule, interval-selection rule, multiplicity family + size, power design point, span-match mode, scoring family, tie/exclusion rules}`; the final report embeds the pre-reg `{hash, commit_sha}` and a `pre_registration_matches: true` check, and states the **run-lineage count** (# prior pre-registered runs for the same system set, so re-roll-until-favorable is visible). Ordering is **tamper-evident only via the commit anchor** (a back-datable local timestamp is at most a secondary check). *Measure:* pre-reg artifact field-completeness check + **commit-on-remote oracle (`git branch -r --contains <sha>` non-empty against the configured origin, or a remote-API equivalent — a local-only SHA fails)** + hash-chain assertion + report-embeds-hash + lineage-count present (CI-blocking when the run-type requires pre-reg). *Serves:* AX-pii-anon-005 (element 4); UC-18, UC-19 (parity).

- **NFR-032 — Pre-registration parity for full-corpus (census) runs.** *Threshold (boolean-auditable):* a full-corpus run, when its run-type requires pre-registration, **freezes systems / metrics / analysis plan before scoring exactly as a sampled run does** (a census still has researcher degrees of freedom). *Measure:* a pre-reg exists for the full-corpus run and passes NFR-031's equality set. *Serves:* AX-pii-anon-005; UC-18/UC-19 parity (REPRO-04).

- **NFR-033 — Run-type designation scopes the enforced rigor bar.** *Threshold (boolean-auditable):* the workflow exposes a **declared run-type** (e.g. `smoke` / `dev` / `leaderboard-submission` / `filing-grade`) as a **CLI flag/preset** that deterministically selects (a) the enforced AX-005 bar, (b) the backing sample mode, and (c) the pre-registration gate. The mapping is **fixed and auditable**: `leaderboard-submission` + `filing-grade` ⇒ **full** AX-005 bar + pre-reg required; `smoke`/`dev` ⇒ **CI + p-value emission suppressed** (no number to mis-cite). Pre-registration is **never a single global on/off**. *Measure:* **CI gate (blocking):** a table-driven test reads the **committed run-type→profile enum/fixture** and asserts every member maps to a **total** triple `{rigor_bar ∈ {full-AX005, suppressed}, sample_mode ∈ {full, sample}, prereg ∈ {enforced, opt-in, off}}` with **no field unset**, AND the behavioral invariants hold (a `full-AX005` member omitting any AX-005 element fails; a `suppressed` member emitting any non-null CI/p-value fails). Reviewer audit of the preset table is **secondary, non-gating**. *Serves:* AX-pii-anon-005 (enforcement scope); DF-1/DF-2; resolves the §4 universality tension (INT-02 vs INT-05).

- **NFR-034 — Smoke runs are statistically inert (suppress all CI + p-value fields).** *Threshold (boolean-auditable):* a `smoke` artifact carries `preset == "smoke"` + a **non-strippable** `not_statistically_valid: true` caveat **AND every CI field and every p-value field is suppressed/null** — a smoke output can never *look* inferential. *Measure:* linter asserts smoke artifacts contain zero non-null CI/p-value fields + the non-strippable flag (CI-blocking). *Serves:* AX-002; AX-pii-anon-005 (enforcement scope); UC-20 (STATS-09).

## F. Powered, lattice-stratified sampling (the precondition P3)

- **NFR-035 — Powered-tier compliance OR explicit UNDER-POWERED with a named shortfall (never silently LARGE).** *Threshold (boolean-auditable):* every committed lattice cell is classified **against its REALIZED positive count** (not a `target_n` label) into **`WELL_POWERED` / `UNDER_SAMPLED` / `CORPUS_LIMITED` / `EMPTY`** (+ **`NOT_ASSESSED`** for critical types outside the coverage envelope), each `target_n` **reproduced from its design-point triple** `(p_ref, half_width, α)` via `required_n` per `stats/power.py::TIER_SPECS` — **CRITICAL 1,522** `(0.99, 0.005)`, **STANDARD 753** `(0.98, 0.010)`, **LONG_TAIL 200** `(0.95, 0.03025)`; targets **derived, never hand-typed**. The run is labelled with the `PowerMatrix.verdict()` — **SMALL / ADEQUATE / LARGE** (LARGE iff well-powered fraction `≥ 0.999`; ADEQUATE iff `≥ 0.80`; else SMALL — verified `stats/power.py::PowerMatrix.verdict` L351-361) — and the **representative default is never silently reported as LARGE**: an under-tier run reads ADEQUATE/SMALL with its named shortfall. *Measure:* every cell carries a `power_class` with `target_n` reproduced from its triple + a named shortfall on every under-tier cell; `PowerMatrix.verdict()` present in the manifest; no cell unclassified or silently under-tier. *Serves:* **P3**; AX-003; UC-16.

- **NFR-036 — Shortfall expressed in REALIZED positive counts, per (entity-type × language) cell.** *Threshold:* every under-tier cell names its shortfall as **"short by N realized positives vs the {WELL_POWERED|ADEQUATE} threshold"** (an integer count), per `(entity_type × language)` cell — **not** a `target_n` label and **not** prose like "needs more data". The shortfall is **structurally part of the number / non-strippable** ("the stamp gets lost in the filing", INT-04). *Measure:* linter asserts each under-tier cell has an integer `realized_positive_shortfall` + the non-strippable binding; reuses `scripts/lattice_audit.py::deficits` (returns `observed` + `deficit` per committed cell). *Serves:* **P3**; AX-003; UC-16/UC-23(d); DF-3.

- **NFR-037 — UNDER_SAMPLED vs CORPUS_LIMITED vs EMPTY distinction surfaced (fixable vs irreducible).** *Threshold (boolean-auditable):* the manifest and report distinguish **`UNDER_SAMPLED`** (the full corpus has enough positives — fixable by drawing more) from **`CORPUS_LIMITED`** (the full corpus itself has fewer positives than `target_n` — irreducible) from **`EMPTY`** (n == 0, structural). A `CORPUS_LIMITED` cell is **never** labelled as merely under-sampled. *Measure:* classification check that compares cell realized positives to both the sample draw and the full-corpus positive total (`lattice_audit.audit_positives` over the full corpus) (CI-blocking). *Serves:* **P3**; AX-003; UC-16 (MEI-03); INT-03/INT-06.

- **NFR-038 — Sampler performance: single streaming pass, bounded memory, deterministic.** *Threshold (number + unit):* the sampler counts committed-cell positives in **exactly ONE streaming pass** over the corpus (generator-based, **never** materializing the full corpus); working-set memory is **bounded by the number of committed cells (730 @ `47c3a8f`), NOT the corpus cross-product** — i.e. **O(#committed-cells) ≈ O(730) keys, O(1) per-annotation candidate-key lookups**, independent of the 575,604 record count; and the selection is **byte-reproducible** under a fixed seed (Algorithm-R reservoir). *Measure:* **primary gate (deterministic, RSS-noise-immune):** a **data-structure-size assertion** that the sampler's candidate-key working set satisfies **`len(keys) ≤ 730`** (the committed-cell count, the true O-bound) and that this holds across corpus sizes (10k vs 100k vs full) — an integer assertion, not raw RSS; **plus** a pass-count assertion (corpus opened **exactly once**). **Secondary signal (non-gating):** peak-RSS growth between a 10k-record and a 100k-record run stays **within a stated tolerance `≤ 1.10×`** (replacing the flaky "constant"). Grounded in `scripts/lattice_audit.py::audit_positives` ("single streaming pass… key set bounded by #committed-cells, not the cross-product") and `scripts/benchmark_throughput.py::reservoir_sample` (Algorithm-R single-pass, seeded under `SEED_BENCHMARK`). *Serves:* **P3**; AX-002. *(`real_user_needed: false` — this is a code-structural bound, not a wall-clock floor.)*

- **NFR-039 — Design point + coverage envelope written into the sample manifest.** *Threshold (boolean-auditable):* the sample manifest records the **power design-point triple** `(p_ref, half_width, α)` per tier **AND** the **entity-type coverage envelope** (which of the 63 types × 60 languages × 5 domains are committed), so uncovered critical types (e.g. MRN, IBAN) surface as **`NOT_ASSESSED`** rather than being implicitly claimed as powered. *Measure:* manifest field-completeness check (design point present + coverage envelope present + every uncovered critical type listed `NOT_ASSESSED`). *Serves:* **P3**; AX-003; UC-16 (DOM-03/C10).

## G. Dataset-seam reconciliation + regression contract (the precondition P2)

- **NFR-040 — Dataset seam reconciled to v2.0.0 via a versioned, validated crosswalk (lossless OR explicit-lossy).** *Threshold (boolean-auditable):* `pii-rate-elo` loads PII-Anon **v2.0.0** — "correctly" defined operationally as the conjunction **`version == "2.0.0"` AND `license ∈ {CC0-1.0, CC0}` AND `record_count == dataset.record_count("2.0.0")` (== 575,604 for the full corpus) AND the citation string references v2.0.0 AND the crosswalk lossless/explicit-lossy test is green** — the metadata pins corrected from the values verified at HEAD: converter `num_records=159891` (`pii_anon_eval.py:93`) → **575,604**, `license="CC-BY-4.0"` (L221) → **CC0**, `citation="…v1.3.0"` (L222) → **v2.0.0**, and `schema.py` `version="1.3.0"` → **2.0.0** — through a **versioned crosswalk artifact** carrying a **lossless test OR an explicit-lossy test** (no silent field loss). *(Verified nuance: the row parser already tolerates `annotations` with a `labels` fallback — `schema.py`/L349-style normalizer — so this is **metadata-provenance reconciliation, not field-reading**.)* *Measure:* load-through-`pii-rate-elo` reports `version == "2.0.0"` and yields 575,604 (full corpus); crosswalk lossless/explicit-lossy test green. *Serves:* **P2**; UC-21.

- **NFR-041 — Regression contract pins the seam (5-tuple) and fails red on drift.** *Threshold (boolean-auditable):* a committed regression test turns **red** if **any** of `{version pin, record-count pin (575,604), entity-type count = 63, schema fingerprint (annotation keys), dataset content hash}` reverts/mutates from the v2.0.0 values — guarding **both** a regression to v1.3.0 / 159,891 **and** a same-count corpus with a mutated annotation schema or **silently swapped content**. Pins are the canonical values from eval-data `tests/test_doc_drift.py`: `CANONICAL_RECORDS="575,604"`, `CANONICAL_ANNOTATIONS="2,486,438"`, `CANONICAL_VERSION="2.0.0"`, `CANONICAL_ENTITY_COUNT=63` (**derived** from `taxonomy.ENTITY_TYPE_COUNT`, never hardcoded), plus schema fingerprint + dataset content hash. *Measure:* the regression test mutates each pin in turn and asserts red (CI-blocking). *Serves:* **P2**; UC-21 (REPRO-05/DOM-06/GAME-07).

## H. Observability — run-record + file-level provenance (AX-005 element 4)

- **NFR-042 — One provenance-complete run-record per spine stage, chained by a shared run id.** *Threshold (boolean-auditable):* a single end-to-end run emits **one run-record per spine stage** (`load`, `sample`, `run`, `score`, `rate`, `report` = **6 records**), each stamped with **`{dataset_version, record_count, schema_fingerprint, seed, stage, code_commit}` + a toolchain fingerprint**, all sharing **one run id** so the chain is traversable. A missing stage record, an unlinked record, or an absent `code_commit` fails the check. This realizes INT-05's "first-six hard send-back" provenance floor (dataset version + count + schema fingerprint; seed + code_commit + toolchain). *Measure:* run emits exactly 6 stage records, each field-complete, sharing one run id (CI-blocking); reuses the `scripts/benchmark_throughput.py` run-record schema + `scripts/write_manifest.py` pattern. *Serves:* AX-002; AX-pii-anon-005 (element 4); UC-22 (REPRO-06).

- **NFR-043 — File-level provenance on every emitted artifact.** *Threshold (boolean-auditable):* **every** emitted artifact (manifest, sample, results, leaderboard, figures, pre-reg) carries **file-level provenance** — the producing `{run id, stage, code_commit, content hash}` — so any artifact can be traced to the exact run + executable that produced it. *Measure:* artifact-field presence check across all emitted files + a `MANIFEST.sha256`-style content-hash index (`scripts/write_manifest.py`). *Serves:* AX-pii-anon-005 (element 4); UC-22 (C5); INT-05/N-04.

## I. Honest reporting — caveat, operating point, anti-over-claim (AX-001/003/005 elements 5 & 6)

- **NFR-044 — Non-strippable synthetic-only / anti-anonymity caveat on EVERY published metric and figure.** *Threshold (boolean-auditable):* the **synthetic-only / anti-anonymity caveat rides every emitted metric and every emitted figure and CANNOT be serialized away** — enforced **by construction** (the `scoring/detection.py::DesignProvenance` non-strippable pattern; `__post_init__` raises on an empty caveat). Stripping the caveat from any artifact makes that artifact **invalid**. *Measure:* round-trip serialize/deserialize test asserts the caveat survives on every artifact; attempt-to-empty raises (CI-blocking). *Serves:* AX-001/003; AX-pii-anon-005 (element 5); UC-23(c); the SPY/NAACL-SRW-2025 synthetic-over-estimation peer-review precondition.

- **NFR-045 — Anon vs pseudo scored by SEPARATE metric families; pseudo never scored by bare span-F1.** *Threshold (boolean-auditable):* the run **declares `scoring_family ∈ {anonymization, pseudonymization}`** (AX-004); **zero** code paths merge the two families into one headline score (inherits cycle-1 **NFR-005**); pseudonymization is scored by consistency / reversibility / linkage metrics and **never** by bare span-F1. *Measure:* static check (no merge path) + run-record asserts `scoring_family` declared and pseudo runs carry no bare-F1 headline (CI-blocking). *Serves:* AX-004; AX-pii-anon-005; UC-17 (DOM-01); NFR-005 (no-regression).

- **NFR-046 — Operating-point reporting: recall-priority Fβ (β≥2, recorded) / FN:FP cost + AUPRC at the pre-registered operating point.** *Threshold (boolean-auditable):* the report shows, **instead of a lone F1**, (a) a **recall-priority Fβ with `β` RECORDED in the manifest/pre-reg and `β ≥ 2`** (committed default **`β = 2`** — *R10-to-confirm*) **OR a stated integer FN:FP cost ratio** (naming the de-id asymmetry — a missed SSN is a breach, a false positive is over-redaction / the false-positive tax) **AND** (b) a **threshold-free AUPRC** alongside the **pre-registered operating point** whose **recall target is RECORDED in the pre-reg** (committed default **`recall = 0.90`** within INT-04's 0.90–0.93 band — *R10-to-confirm*; threshold rule fixed pre-scoring per UC-18, so the curve is not one point post-hoc-tuned to the eval set). *Measure:* linter asserts {Fβ with `β` recorded and `β ≥ 2`} OR a stated integer FN:FP cost present + AUPRC present + the emitted operating point equals the pre-reg-recorded recall target — a free/undeclared `β` or a post-hoc point fails (CI-blocking). *Serves:* AX-pii-anon-005; UC-23(a); DOM-02 / N-03 (the false-positive-tax driver, P-priv-eng).

- **NFR-047 — Honesty flags travel with the artifact; report is self-verifying (embeds pre-reg hash + run id).** *Threshold (boolean-auditable):* the rendered report carries **all** applicable honesty flags from this **closed, enumerated set** — **RD-NOT-CONVERGED** (from NFR-029), **UNDER-POWERED / UNDER-SAMPLED / CORPUS-LIMITED** per cell (NFR-035/037), **contamination-uncontrolled** per non-`disclosed-unseen` system (NFR-048), **rank-volatility: UNMEASURED** if single-seed (NFR-024), **worst-language recall** = `argmin over the languages in the run's coverage envelope of per-language recall` reported **with the language label realizing the min** (computable), and **low-resource recall** = recall restricted to the **enumerable committed set of languages with realized positives `< 200`** (the LONG_TAIL target) — both replacing the prior undefined "worst-language / low-resource" prose so cross-lingual collapse is not masked by the aggregate — **AND embeds the pre-registration `{hash, commit_sha}` + run id** so it is reproducible-from-manifest end-to-end. **Stripping any signal from the enumerated honesty set, un-gating a rank from its paired test, omitting the AUPRC/Fβ asymmetry view, or dropping the pre-reg-hash + run-id binding makes the artifact invalid.** *Measure:* report linter asserts each applicable enumerated flag present (incl. the worst-language `argmin`+label and the `<200`-set low-resource recall) + the pre-reg/run-id binding present, and that removing any flag fails validation (CI-blocking). *Serves:* AX-pii-anon-005; UC-23(d/e); REPRO-07/DOM-07.

## J. Leaderboard hygiene + anti-gaming / contamination (AX-005 element 6)

- **NFR-048 — Contamination disclosure per system; `unknown` rejected; held-out non-exposure attested.** *Threshold (boolean-auditable):* every system carries **`contamination_status ∈ {disclosed-unseen, disclosed-trained-on-corpus, undisclosed}`**; a leaderboard entry with `contamination_status: unknown` is **rejected/flagged** (not silently accepted — "a leaderboard that accepts unknown is not trustworthy", INT-02); leaderboard/published entries additionally require a **signed attestation that held-out labels were not exposed** (INT-03's 6th AX-005 element); any non-`disclosed-unseen` rating is flagged **contamination-uncontrolled** downstream. *Measure:* submission validator rejects `unknown`; attestation-presence check for leaderboard run-types; downstream flag propagation asserted (CI-blocking). *Serves:* AX-pii-anon-005 (element 6); UC-17(d)/UC-23(d); the `P-tool-vendor` gaming threat-model.

- **NFR-049 — Leaderboard submission hygiene: held-out labels protected + provenance-stamped/blind submission.** *Threshold (boolean-auditable):* leaderboard submission keeps **held-out labels protected** (not exposed by the scoring API) and is **provenance-stamped / blind** (submitter cannot leak the held-out set or read it back). *Measure:* **CI gate (blocking):** **(i)** an **API-surface test** — a call sequence to the scoring endpoint returns a response whose schema **excludes the held-out label field** (labels never returned), AND **(ii)** a **submission-provenance log record present and well-formed**. The governance checklist audit is a **secondary, non-gating** human corroboration. *Serves:* AX-pii-anon-005; NFR-014 (cycle-1 governance neutrality, extended); UC-17/UC-23; Open Item 9.

## K. Engineering integrity — pure-stdlib cores, lazy heavy-dep guards, gates green

- **NFR-050 — Pure-stdlib statistical cores with lazy heavy-dependency guards.** *Threshold (boolean-auditable):* the statistical cores on the run path (`stats/intervals.py`, `stats/paired.py`, `stats/power.py`, `stats/lattice.py`) import **only the Python standard library** (`math`, `random`, etc. — **no** `numpy` / `scipy` on the CI/paired/power path); any heavy dependency (spaCy/Presidio/transformers) sits behind a **lazy guard** so importing the assessment modules never imports it (the `benchmark_throughput.py::_spacy_ner_detector` pattern — import inside the function, clear `[baselines]` install error). *Measure:* import-graph test asserts the cores import no third-party numeric lib + a "module import triggers no heavy import" test (CI-blocking). *Serves:* AX-002 (determinism — no global-RNG numeric libs); NFR-004 pattern; no-regression guardrail.

- **NFR-051 — `pii-rate-elo` consumer gates stay green (pytest + ruff + mypy).** *Threshold (boolean-auditable):* after the seam reconciliation + run-path rewiring, `pii-rate-elo`'s **own** gates remain green — `pytest`, `ruff`, `mypy` (declared in its `pyproject.toml`). *Measure:* run the three gates in the `pii-rate-elo-pipeline` venv; all pass. *Serves:* OWNS/CONSUMES boundary (do-not-break-the-consumer); no-regression guardrail.

## L. Cycle-1 no-regression guardrails (encoded as binding CAP-02 NFRs)

- **NFR-052 — Lattice frozen: 730 cells @ `47c3a8f` (`lattice --check` green).** *Threshold (number, boolean-auditable):* `python -m pii_anon_datasets.stats.lattice --check` reports **exactly 730 cells** matching `build_committed_lattice()` at commit **`47c3a8f`** (verified green this session: "OK … 730 cells"). *Measure:* the `--check` round-trip in CI (exits non-zero on any cell-count or content drift). *Serves:* NFR-018 anti-drift (inherited); no-regression guardrail; **no corpus regeneration**.

- **NFR-053 — NFR-018 committed-lattice power gate stays ON.** *Threshold (boolean-auditable):* `scripts/validate.py --lattice` keeps auditing positives per committed cell in **one streaming pass** and **exits non-zero on any shortfall** (CI-blocking); the inherited cycle-1 **NFR-018** gate is **not** weakened, disabled, or scoped away by CAP-02. *Measure:* `validate.py --lattice` green in CI + a test that re-enables it if disabled. *Serves:* NFR-018 (inherited, hard gate); **P3** (power floor); no-regression guardrail.

- **NFR-054 — Documentation drift = 0 (NFR-013 stays green).** *Threshold (number):* record count (575,604), annotation count (2,486,438), entity-type count (63), and version (2.0.0) are **identical across all canonical docs (zero drift)**; `pytest -k nfr_013` (the `test_doc_drift.py` suite) stays green; tags `v1.3.0` + `pre-lattice-enrichment` intact. *Measure:* the doc-drift cross-doc consistency suite in CI. *Serves:* NFR-013 (inherited); no-regression guardrail.

- **NFR-055 — Four metric families never merged (NFR-005 invariant holds across CAP-02).** *Threshold (boolean-auditable):* **zero** code paths added by CAP-02 merge the four metric families (detection / anonymization / pseudonymization / re-id) into one headline score; the inherited **NFR-005** boolean invariant holds. *Measure:* **CI gate (blocking):** a **static check over the CAP-02-added module set** asserting **zero** code paths merge the four metric families into one headline score (the NFR-005 boolean invariant). Reviewer audit is a **secondary, non-gating** corroboration. *Serves:* AX-004; NFR-005 (inherited); no-regression guardrail.

---

## Coverage — every brief-mandated NFR theme maps to ≥1 NFR

| Brief-mandated quality attribute | NFR(s) |
|---|---|
| Byte-reproducibility of sample + run from manifest | **NFR-030** |
| Powered-tier compliance OR explicit UNDER-POWERED + named shortfall (realized positives) | **NFR-035, NFR-036, NFR-037** |
| Every metric carries a CI (interval-selection rule) | **NFR-023** (+ NFR-024/025 scope) |
| Paired test for system-vs-system (Holm-Bonferroni + 2 inference families) | **NFR-026, NFR-027, NFR-028** |
| RD reported as achieved max-RD ± 2RD | **NFR-029** |
| Pre-registration completeness (opt-in) + universal manifest-reproducibility | **NFR-031, NFR-032, NFR-033** (opt-in/run-type) + **NFR-030** (universal) |
| Run-record provenance completeness + file-level provenance | **NFR-042, NFR-043** |
| Non-strippable synthetic-only caveat on every metric | **NFR-044** |
| Statistics-core integrity (audited only; significance.py quarantined; zero approximated stats on run path) | **NFR-019, NFR-020, NFR-021, NFR-022** |
| Sampling performance (single streaming pass; bounded memory; deterministic) | **NFR-038** |
| Pure-stdlib cores + lazy heavy-dep guards | **NFR-050** |
| Cycle-1 guardrail no-regression (lattice 730@47c3a8f, NFR-018 gate, doc-drift 0, four families separate, no corpus regen) | **NFR-052, NFR-053, NFR-054, NFR-055** |
| `pii-rate-elo` gates green | **NFR-051** |
| Anon vs pseudo separate families (AX-004) | **NFR-045** (+ NFR-055) |
| Operating-point reporting / false-positive tax | **NFR-046** |
| Honest verdicts ride the artifact (self-verifying report) | **NFR-047** |
| Leaderboard hygiene + contamination disclosure | **NFR-048, NFR-049** |

## Precondition → NFR map (the 3 hard preconditions, fully covered)

| Precondition | NFR(s) |
|---|---|
| **P1 (SHOWSTOPPER)** — audited stats core only; `significance.py` quarantined; zero approximated stats on run path | **NFR-019, NFR-020, NFR-021, NFR-022** |
| **P2** — dataset seam → v2.0.0 via versioned crosswalk + regression contract | **NFR-040, NFR-041** |
| **P3** — powered lattice-stratified sampler emitting PowerMatrix verdict + per-cell named shortfall vs realized positives | **NFR-035, NFR-036, NFR-037, NFR-038, NFR-039** (+ NFR-053 power gate) |

## Axiom → NFR map (AX-pii-anon-005 elements)

| AX-005 element | NFR(s) |
|---|---|
| (1) CI with method named | NFR-023, NFR-024, NFR-025 |
| (2) Paired test + declared multiplicity family (Holm-Bonferroni, 2 families, family size in text) | NFR-026, NFR-027, NFR-028 |
| (3) Convergence as achieved max-RD ± 2RD + rounds | NFR-029 |
| (4) Provenance reproducible-from-manifest | NFR-030, NFR-031, NFR-032, NFR-042, NFR-043 |
| (5) Non-strippable synthetic-only caveat | NFR-044 |
| (6) Contamination disclosure + held-out non-exposure attestation | NFR-048, NFR-049 |
| Enforcement scope (run-type-bound) | NFR-033, NFR-034 |

---

## Count + methodology

**Count: 37 new NFRs (NFR-019 … NFR-055).** Cumulative with cycle-1: NFR-001 … NFR-055 (NFR-001…018 inherited, still binding).

- **Threshold-grounding (direct file read at HEAD, 2026-06-01):** `stats/power.py` (`TIER_SPECS` 1522/753/200, `REID_TIER_SPECS` 897/385, `PowerClass` WELL/UNDER/EMPTY, `PowerMatrix.verdict()` LARGE≥0.999 / ADEQUATE≥0.80 at L351-361); `stats/intervals.py` (`_require_int_counts` reidx-02 guard L39-51; Wilson/Clopper-Pearson); `stats/paired.py` (`mcnemar_exact`/`mcnemar_chi2`/`paired_bootstrap_recall_delta` with LOCAL `random.Random(seed)` L140); `scripts/lattice_audit.py` (single streaming pass; key set bounded by #committed-cells; `deficits` observed+deficit); `scripts/benchmark_throughput.py` (`reservoir_sample` Algorithm-R single-pass; `SEED_BENCHMARK=20260531`; run-record schema; `CANONICAL_RECORDS=575604`); `stats/lattice.py --check` (730 cells @ `47c3a8f`, run green this session); `tests/test_doc_drift.py` (CANONICAL pins 575,604 / 2,486,438 / 2.0.0 / 63-derived); `pii-rate-elo/analysis/significance.py` (fabricated `se=…/100`+`np.random.normal` L205-206; `n_approx=100`/`pooled_sd=0.1`/`z_stat` L271-281; instantiated `cli.py:242` behind `run_significance_tests` `cli.py:240`); `pii-rate-elo/datasets/converters/pii_anon_eval.py` (`num_records=159891` L93, `license="CC-BY-4.0"` L221, `citation="…v1.3.0"` L222, `max_samples` head-truncation L82); `pii-rate-elo/pyproject.toml` (pytest/ruff/mypy gates).
- **Interview-grounding (R3 synthesis, AGENT_SIMULATED):** the AX-005 sharpenings (multiplicity-family-in-text; 6th contamination element; achieved max-RD ± 2RD + rounds; run-type-scoped enforcement) and the run-type designation mechanism are carried verbatim from `interview-synthesis.md` (INT-01…INT-06; the §4 universality adjudication; INT-05's first-six send-back floor → NFR-042/043).
- **`real_user_needed` flags (Pass-2):** NFR-038 is a code-structural bound (no real-user number needed). Inherited **NFR-010b** (≥5,000 rec/sec lightweight-detection floor on an 8-core reference host) remains `real_user_needed: true` / canonical verdict **INSUFFICIENT_EVIDENCE** until a real reference-host run lands out-of-band (per `benchmark_throughput.py` protocol §2) — CAP-02 does not re-pin it; it inherits it unchanged.
- **provisional_status: AGENT_SIMULATED.** Thresholds are agent-authored; the code/line evidence is firsthand. Real-user validation of these thresholds is a Pass-2 follow-up. **R10 threshold-validation** of the quantified NFRs (esp. NFR-038 memory bound, the CI/paired-test linter thresholds) to be recorded in `_threshold-validation/`.

✅ **R4 NFR authoring complete (2026-06-01).** 37 new Quality Attributes (NFR-019…NFR-055), each with a quantified/boolean-auditable threshold + measurement method + the axiom/precondition it serves; the full academic-soundness bar + the 3 hard preconditions (P1/P2/P3) + the no-regression guardrails encoded as testable NFRs; **AX-pii-anon-005 PROPOSED and recorded for confirmation** with the two cohort-demanded sharpenings + run-type-scoped enforcement, with every NFR wired to it. Offsets honored (NFR→019, continued global numbering, none reused). `provisional_status: AGENT_SIMULATED`.
