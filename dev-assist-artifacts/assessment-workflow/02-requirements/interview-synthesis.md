# CAP-02 Requirements R3 — Simulated Interview Synthesis

**Capability**: CAP-02 — Powered, repeatable, reportable assessment **workflow** (runs the **existing** `pii-rate-elo` tournament against PII-Anon **v2.0.0** over a **powered, lattice-stratified sample (default)** / **full corpus (opt-in)** / **smoke (fast)**, with epistemic instrumentation + reporting at every spine stage: `load → sample → run systems → score → rate → report`).
**Stage**: assessment-workflow / 02-Requirements · Phase R3 (simulated-interview synthesis)
**Date**: 2026-06-01
**Inputs**: 6 simulated interviews dispatched against the R2 interview-guide.md (18 persona-agnostic core probes + per-persona Layer-2 + sub-archetype probes). INT-01 P-acad-deid (clinical/PHI de-id) · INT-02 P-mlnlp-researcher (methods-author/leaderboard) · INT-03 P-tool-builder (OSS Presidio/GLiNER maintainer) · INT-04 P-priv-eng (pre-screen/procurement) · INT-05 P-dpo / P-complreviewer (external assessor, Art-11) · INT-06 P-agentic-redteam (enterprise red-teamer, recognition-oracle lens).
**provisional_status**: AGENT_SIMULATED — every interviewee is an agent-simulated single-session respondent; the *code/version/line evidence* they reason against is the Discovery file-read at HEAD on 2026-06-01. **Agent-simulated research is NOT a substitute for real users.** Real-user interviewing with the same guide is a Pass-2 activity. Two structural caveats inherited from R2: (1) the **P-dpo / assurance lens (INT-05) is FIRST CONTACT**, not re-confirmation of the cycle-1 P-dpo priority — a clean result here does not validate that priority; real-user DPO interviews remain a Pass-2 must. (2) The whole roster is **representative-scale, single-session** — directional, not saturated.

> **Vocabulary remap (project framing — synthetic PII benchmark).** Persona = assessment/benchmark **consumer**; UC = **Evaluation Scenario**; FR = **Benchmark/Assessment Capability**; NFR = **Quality Attribute**; DC = **Benchmark Component**.

> **ID discipline (load-bearing).** The interviewees referenced FR/NFR numbers **inconsistently and in conflict** (e.g., INT-02/03/04 each minted overlapping `FR-030…FR-04x` / `NFR-019…NFR-02x` for *different* capabilities; INT-01/INT-06 referenced UC IDs only). **This synthesis does NOT canonicalize those clashing FR/NFR IDs** — that is the R4 FR/NFR-author's job (offsets reserved: **FR→030, NFR→019**, continue global numbering, never renumber/reuse). To stay decision-stable, this synthesis anchors every signal to the **already-stable** IDs: **UC-16…UC-23**, the five decision-forks **DF-1…DF-5**, the candidate axiom **AX-pii-anon-005** (and inherited AX-001/002/003/004), the concept-value items **C1…C10**, **Open Items 1–13**, and the anti-over-claim rails **G1–G7**. Where a net-new capability has no home yet, it is named descriptively and tagged `→ R4 (new FR/NFR candidate)`.

---

## 0. Coverage map (who answered, what they gate)

| Interviewee | Persona (sub-archetype) | Priority | Probes | Primary lens |
|---|---|---|---|---|
| INT-01 | P-acad-deid (clinical i2b2/n2c2) | HIGH | 29 (18 core + 11) | Citation legitimacy; synthetic ceiling; DOI gate |
| INT-02 | P-mlnlp-researcher (methods-author) | HIGH | 29 (18 core + 11) | Reviewer-3 survival; seam-pinning; rank stability |
| INT-03 | P-tool-builder (OSS maintainer) | HIGH | 28 (18 core + 10) | CI-gate viability; smoke speed; F2/AUPRC; contamination |
| INT-04 | P-priv-eng (pre-screen) | MEDIUM | 27 (18 core + 9) | Operating-point / false-positive tax; taxonomy crosswalk; neutrality |
| INT-05 | P-dpo / P-complreviewer (external assessor) | MEDIUM (**first contact**) | 30 (18 core + 10 + 2) | Art-11 filing; anon/pseudo legal end-states; non-strippable per-metric; arms-length |
| INT-06 | P-agentic-redteam (enterprise) | MEDIUM | 27 (18 core + 9) | Recognition-oracle calibration; throughput SLA; adversarial-distribution gap |

**3 HIGH (INT-01/02/03) = the acceptance + credibility cohort.** The academic-soundness bar is built for their standards; where all three converge, the signal is gating. The 3 MEDIUM (INT-04/05/06) are real but downstream — and each surfaced a **net-new capability the HIGH cohort did not** (operating-point, Art-11 format, throughput SLA), which is the most valuable MEDIUM contribution.

---

## 1. Convergent priorities (multi-persona, gating)

Ordered by breadth + strength of convergence. A signal is **UNANIMOUS** when all 6 named it, **HIGH-CONVERGENT** when all 3 HIGH personas named it.

### C-1 · Audited stats path is non-negotiable — quarantine `significance.py` (P1 precondition) — UNANIMOUS, anxiety=strong
Every interviewee independently treated routing through the **audited `eval-data/stats` core** (`paired.py` + `intervals.py`) and **quarantining the fabricated `pii-rate-elo/analysis/significance.py`** (bootstrap `se=metric*(1-metric)/100` + Gaussian noise L205-206; McNemar `n_approx=100`/`pooled_sd=0.1` L271-281) as a **walk-away trigger**, not a preference. INT-01: "if I audit the significance code and find hardcoded n=100 or Gaussian noise … I walk away and I tell my colleagues." INT-02: "if any significance computation routes through `significance.py` … my paper is compromised." INT-03 (tool-builder, surprised himself): "matters to me … more than I expected … I'm publishing a number derived from noise." **Threshold signal**: INT-02 and INT-03 both demanded this be **CI-enforced, not just documented** — a test asserting the audited path is always taken and the fabricated path is **unreachable from the run surface**. → sharpens **UC-17**, **Open Item 1**; `→ R4 (new NFR candidate: run-surface quarantine assertion test)`.

### C-2 · Dataset-seam regression contract — silent drift is a confirmed historical burn (P2 precondition) — HIGH-CONVERGENT + INT-04/05, anxiety=strong
The seam reconciliation to **v2.0.0 / 575,604 / CC0 / `annotations`** with a **regression contract** (version + record-count + 63-type count + schema fingerprint + **dataset content hash**) is confirmed by 5 of 6, and two reported a **real multi-period burn from silent dataset drift**: INT-01 lost "a 6-month reanalysis"; INT-03 "spent a week tracing which changes drove the delta" after shipping a model-card number computed against the wrong version. INT-02 named it "the single most likely trust-killer." **Threshold signals**: (a) the contract must catch **same-count substitution** (content hash), not only count/version reverts (INT-03, GAME-07); (b) INT-02 wants the contract emitted as a **structured field in the run-record JSON** — assertable in a paper's methods section, not just a crosswalk doc; (c) INT-03 wants **corpus-vs-model delta attribution** when a score moves across runs. → sharpens **UC-21**, **C1**; `→ R4 (new FR candidate: run-record regression-contract field; delta attribution)`.

### C-3 · Per-metric CIs with a deterministic interval-selection rule — UNANIMOUS (incl. the oracle lens) — pull=strong
A CI on every published proportion is the cheapest credibility the benchmark can buy, and all 6 confirmed it — including INT-06, who reframed it for the oracle use ("if I know the oracle has 94% recall on SSN (Wilson CI 91–96%) I can calibrate my false-negative rate"). INT-04 has built Wilson/Clopper-Pearson "once from scratch; it took two days and a stats review." **Threshold signal**: the **interval-selection rule must be disclosed** (Wilson default / Clopper-Pearson small-n/boundary / paired-bootstrap for differences) — INT-01 and INT-05 both flagged that an undisclosed rule makes the CI column ambiguous. INT-05 lists per-metric CI with disclosed method among the **first-six hard send-back triggers**. → confirms **UC-17 (a)**, **AX-003**, **Open Item 5**.

### C-4 · Paired test before any system-vs-system claim, with declared multiplicity family — HIGH-CONVERGENT + INT-05, push=strong
Eyeballed F1-deltas are a confirmed rejection vector: INT-01's delta "evaporated (p=0.14)" under McNemar in revision; INT-02's ACL submission was killed by Reviewer-3 ("delta is within noise, no significance test") at a **6-week** cost. All three HIGH personas + INT-05 want McNemar + paired bootstrap gating directional claims. **Threshold signal (sharpens AX-005)**: INT-01 and INT-02 independently demanded the **multiplicity family be declared explicitly** — the **two inference families** (exploratory per-cell uncorrected vs confirmatory pairwise **Holm-Bonferroni with family size stated**) must be in the axiom/report text, **not only in code**, "because reviewers will ask and the author needs to be able to explain it without reading source code" (INT-01). → confirms **UC-17 (b)**, **UC-23 (b)**; sharpens **AX-pii-anon-005**.

### C-5 · Named shortfall on under-powered cells (DF-3) — UNANIMOUS, and the study's strongest *delight* — pull=strong
**DF-3 resolved with zero dissent**: all 6 chose **show-greyed/flagged-with-the-exact-shortfall-named**, and all 6 explicitly rejected **both** "omit" (a lie of omission; "looks like you had power you didn't" — INT-01; "looks like the pipeline failed to run on that type" — INT-05) **and** "show like any other number" (it gets cited unqualified). This is the single most-praised feature across the cohort and the resolution of the DF-3 fork. **Threshold signals**: (a) the shortfall must be expressed in **REALIZED positive counts, not target_n labels** — INT-03: "'short by 47 realized positives vs ADEQUATE threshold' is actionable; 'needs more data' is not"; INT-06 wants it per (entity-type × language) cell ("need N more positives on IBAN/es-ES"); (b) the distinction **UNDER_SAMPLED vs CORPUS_LIMITED vs EMPTY** matters (INT-03, INT-06 — fixable-by-drawing-more vs irreducible); (c) INT-01/05 require the label be **structurally part of the number / non-strippable**, because "the stamp gets lost in the filing" (INT-04's Japanese-data case: he flagged it verbally, the reviewer "cited the number unqualified in their risk memo"). → confirms **UC-16**, **UC-23 (d)**; resolves **DF-3 (Open Items 3,5)**.

### C-6 · Non-strippable synthetic-only caveat, enforced by construction — UNANIMOUS, anxiety=strong (assurance/procurement) — pull=strong
All 6 confirmed the **non-strippable synthetic-only / anti-anonymity caveat** (`DesignProvenance` / `ReidProvenance.__post_init__` raises on empty caveat; AX-001/003). The two consumers who *receive* artifacts rather than run them (INT-04, INT-05) escalated it to **anxiety=strong**: INT-04 watched "a colleague present a synthetic leaderboard rank in a vendor review meeting as if it were a POC result … It wasn't caught"; INT-05 makes a strippable caveat the **#1 of four disqualifying failure modes** for a compliance file. **Threshold signal (sharpens C7/format)**: the caveat must survive **export / copy-paste into Confluence / Excel / a regulatory system** and be **inline per-metric**, not a removable header, footer, or appendix (INT-04, INT-05). → confirms **UC-23 (c)**, **AX-001/003**; sharpens **C3/C7**.

### C-7 · Pre-registration is OPT-IN, scoped by run-type (DF-2) — UNANIMOUS resolution — see §3 for the nuance
All 6 converged on the **same** resolution of DF-2: **opt-in for development/internal runs, enforced for the publication/leaderboard/filing run-type.** Nobody wanted mandatory-for-all-runs; everybody wanted it available and enforced where stakes warrant. This is a convergent *resolution*, but because each persona drew the "stakes" line at a different run-type, the **mechanism** (a run-type-scoped gate) is the requirement — detailed in §3. → resolves **DF-2 (Open Item 6)**; confirms **UC-18**.

### C-8 · Seeded / byte-reproducible-from-manifest — UNANIMOUS, table-stakes — pull=strong (habit=low → easy adoption)
Every interviewee named seeded byte-reproducibility (AX-002) as a top trust gate. INT-03's loyalty hook is "file a GitHub issue with a seed, you can repro the failure." INT-01's smallest un-reproducibility trigger is a **missing seed**; second is **undeclared span-match mode**; third is **no corpus version pin**. **Threshold signal**: reproducibility must hold **from the manifest** including the **RNG/environment fingerprint + sampler version**, not just the seed integer (INT-01, INT-06; matches UC-16's canonical-form-equality acceptance signal). → confirms **UC-16 acceptance signal**, **AX-002**.

### C-9 · Per-stage observability run-record with `code_commit` + toolchain — HIGH for the assurance/audit consumers, MEDIUM-pull for HIGH cohort
INT-04 (primary on UC-22) and INT-05 both treat the per-stage run-record as **audit-admissibility**: "when I hand a result to security or audit … they will ask 'which file produced this number?' — not 'which run.'" Both demand **file-level provenance** (C5), not run-level-only, for filing-grade artifacts. The HIGH cohort values it (embeds in a paper's methods section — INT-02) but at lower urgency. → confirms **UC-22**, **C5**; INT-05 maps it directly to **EU AI Act Art-11** provenance.

### C-10 · Anon-vs-pseudo scored by separate metric families (AX-004) — confirmed; legally load-bearing for INT-05, persona-stratified salience
INT-05 calls AX-004 "the highest-value architectural decision from a compliance standpoint" — a single "redaction quality" score is "legally incoherent." INT-02 and INT-03 confirm it from the methods side ("collapsing them hides the recognizer being good and the anonymizer leaking context"). **Salience is persona-stratified** (low for the clinical de-id lens INT-01, where de-id is irreversible removal; gating for INT-05). **Threshold signal**: INT-01 and INT-05 both want the **separation rationale written in the report/docs methodology**, not only enforced in code — reviewers will ask "why no combined F1?" and the author needs a one-paragraph answer. → confirms **UC-17 (scoring_family)**, **AX-004**; `→ R4 (doc/report requirement: AX-004 rationale prose)`.

---

## 2. Threshold signals (quantified — feed R4 NFR thresholds + R9 validation)

These are the concrete numbers/criteria the interviewees attached. They are **agent-simulated and illustrative** (each interviewee flagged their own numbers as needing real-user verification in Pass-2), but they are the directional thresholds the NFR-author should propose and the threshold-validator should pressure-test.

| Signal | Threshold stated | Source | Anchors / `→ R4` |
|---|---|---|---|
| **Smoke runtime (CI gate)** | **< 10 min** hard ceiling ("disabled within a sprint" beyond); **< 5 min** preferred; INT-02 wants **< 60 s** for the inner-loop check | INT-03 (primary), INT-02 | UC-20 · C4 · `→ R4 NFR` |
| **Powered run runtime (release-branch gate)** | **< 30 min** | INT-03 | UC-16/17 · `→ R4 NFR` |
| **Smoke output contract** | committed reference JSON, **per-entity-type F1 ± tolerance**, **byte-diffable in CI**, all CI/p-value fields suppressed | INT-03, INT-02 | UC-20 · C4 · `→ R4 FR: smoke reference-output schema spec` |
| **Oracle inline latency** | **≤ 50 ms p95** per record (synchronous inline calls) | INT-06 | C9 · `→ R4 NFR (oracle track / routes to cycle-1 UC-08)` |
| **Oracle batched throughput** | **≥ 500 records/sec** (async) | INT-06 | C9 · UC-23 throughput output · `→ R4 NFR` |
| **Throughput output format** | p95 latency + rps **published in machine-readable output** under stated **hardware + RNG fingerprint** | INT-06 | UC-23 · C9 · `make benchmark_throughput.py output machine-readable` |
| **Rank-stability** | **≥ 2–3 sampling seeds**; report **Kendall-τ** (INT-02 names 3-seed minimum + Kendall-τ; "one draw … is an anecdote"); INT-02 wants τ as a **first-class output field**, not post-hoc | INT-02, INT-03 | UC-17 (`seed_variance_scope`) · UC-23 · STATS-04/GAME-02 |
| **RD-convergence reporting** | report **achieved max-RD ± 2RD** with **number of Glicko rounds stated**; never "converged" hand-wave | INT-02 | UC-17 (c) · UC-23 · G4 |
| **Operating-point view** | **recall-priority Fβ (β>1)** or stated **FN:FP cost** + **threshold-free AUPRC** at the **pre-registered operating point**; never a lone F1. INT-03 ships **F2 + AUPRC** as the actual gate metric; INT-04 needs **precision-at-fixed-recall** (he operates at recall≈0.90–0.93) | INT-03, INT-04 | UC-23 (a) · DOM-02 · the false-positive tax |
| **False-positive tax (the why)** | a real burn: F1=0.91 → at recall 0.93 fixed, precision dropped to **0.71** → "**34k false positives per 10k real entities**" → remediation **3× the license cost** in Q1 | INT-04 | UC-23 (a) · motivates operating-point requirement |
| **DOI / persistent identifier** | **Zenodo DOI (or equivalent)** is a **HARD citation gate**; a GitHub SHA is **insufficient** and link-rots (INT-01 cites a dead-SHA NeurIPS-D&B proceedings link) | INT-01 (hard), INT-02 (confirms) | C2 · `→ R4 (MUST-level quality attribute for the P-acad-deid citation cohort)` |
| **Filing-grade artifact format** | every metric cell carries its **CI + provenance hash + synthetic-only caveat INLINE**; JSON run-record necessary but **insufficient** — the filed artifact format is itself gated | INT-05 (hard gate), INT-04 (file-level provenance) | C3/C7 · UC-22/23 · `→ R4 (new FR/NFR: filing-grade artifact format)` |
| **Academic-vocabulary output** | **LaTeX tabular / CSV** with labeled headers; product verdict language ("**SHIP-WITH-CAVEATS**") must NOT appear verbatim — translate to academic phrasing | INT-01 | C7 · UC-23 · artifact-first register |
| **Contamination disclosure** | per-system `contamination_status ∈ {disclosed-unseen, disclosed-trained-on-corpus, undisclosed}`; **"unknown" rejected/flagged**; INT-03 adds **signed attestation that held-out labels were not exposed** as a 6th AX-005 element; INT-06 wants the flag to **propagate into the oracle API** so callers filter at query time | INT-02, INT-03, INT-06 | UC-17 (d) · UC-23 (d) · DF-5 · `→ R4 (leaderboard-hygiene FR)` |
| **Send-back floor (assurance)** | INT-05's **first-six hard send-back triggers**: (1) dataset version + record-count + schema fingerprint; (2) seed + code_commit + toolchain; (3) per-metric CI + method; (4) per-slice positive count + power verdict + named shortfall; (5) paired test + Holm-Bonferroni where a comparison is claimed; (6) non-strippable per-metric synthetic-only caveat. Items 7–8 (anon/pseudo separation; convergence statement) required for any comparative ranking | INT-05 (P-complreviewer) | UC-18/22/23 · the AX-005 acceptance checklist from the consumer side |

---

## 3. How the decision-forks resolved (DF-1 … DF-5)

### DF-1 · Default mode (powered sample vs full corpus) — **SPLIT BY USE, NOT BY PERSONA. The locked default holds, but the labelling/parity requirements sharpen.**
This is the **one genuine disagreement** in the study, and it splits on **citable-vs-operational use**, not on persona identity:

- **Powered-sample-as-default is CORRECT for the operational/cost-bound path** — confirmed by INT-02 (when LLM-adversary cost binds), INT-03 (per-PR / CI; "I'd probably never run the full corpus"), INT-04 (pre-screen; "a cost I can't justify … every time"), INT-06 (oracle calibration; "whole corpus every time would be overkill"). The **load-bearing condition all four attached**: the **power verdict must be named per cell** — INT-02: "the wrong default is full-corpus-but-unlabeled-power — that's false confidence"; INT-03: "if the label says ADEQUATE and the CI is shown, I'll trust it as a gate."
- **Full-corpus is REQUIRED for the citable/headline/filing number** — INT-01 (a reviewer "will immediately ask 'why didn't you run the whole thing?'"), INT-03 (model-card headline; "'sampled but powered' requires an explanation I'd rather not owe every user"), and most strongly **INT-05 pushed back on the locked brief**: for **filing-grade** evidence he wants **full-corpus as the default**, powered-sample as an explicitly-labelled cost-optimization mode, because power-tier explanation to a regulatory advisor "adds a layer of explanation that should not need to exist."

**Resolution (consistent with the locked brief + G2/G3):** keep **powered-representative as the CLI default** and **FULL = the citable leaderboard**, but the requirement set must make the *citable-vs-pre-screen distinction first-class and unmistakable*: (a) the representative run is **ALWAYS** labelled with its power verdict (SMALL/ADEQUATE/LARGE) + named shortfall, **never silently LARGE** (G3); (b) a **run-type / filing-grade designation** selects which mode backs a citable claim (this is the same scoping mechanism DF-2 needs — see below). INT-05's pushback is **first-contact assurance signal, not a mandate to flip the default** (R2 caveat) — but it **must be carried to R6/R7** as a candidate refinement for the filing-grade run-type, and to Pass-2 ("I'd want to verify whether regulators in practice accept a well-labeled powered-sample artifact" — INT-05's own hedge). → sharpens **UC-19**, **Open Items 3,4**, **G2/G3**; **carry INT-05's full-corpus-for-filing pushback to R6/R7 + Pass-2**.

### DF-2 · Pre-registration (mandatory vs opt-in) — **RESOLVED: opt-in, run-type-scoped. Universal commitment is manifest-reproducibility.**
**Unanimous** (§1 C-7): opt-in for dev/internal, enforced for the high-stakes run-type. The persona-specific "stakes line": INT-01/02/03 → **leaderboard/publication submission**; INT-05 → **filing-grade / Art-11**; INT-04 → **formal procurement doc or compliance filing**; INT-06 → **formal comparative security report**. The disconfirming probes paid off: **3 interviewees independently warned mandatory-for-all-runs would cause avoidance** — INT-02 "would kill adoption for the exploratory loop," INT-03 "developers to bypass the tool," INT-06 "route around the harness entirely," and INT-05 warned teams would "**avoid the formal mode and file development outputs instead, which is worse**." **Requirement (net-new mechanism):** the workflow must support a **run-type designation** (e.g., `dev` / `smoke` / `leaderboard-submission` / `filing-grade`) that **scopes pre-registration enforcement (and the AX-005 bar — see §5)**, rather than a single global on/off. INT-01/03 want this exposed as a **CLI flag/preset**, "not a philosophy" (INT-03). When on: **git-commit-anchored + pushed-to-remote + hash-chained into the first run-record**, immutable, with **run-lineage count disclosed** (re-roll-until-favorable visibility). → resolves **DF-2 (Open Item 6)**; confirms **UC-18**; `→ R4 (new FR: run-type designation scoping pre-reg + AX-005 enforcement)`.

### DF-3 · Under-powered reporting — **RESOLVED with zero dissent: greyed/flagged + named shortfall in realized positives.** (Full detail in §1 C-5.)
The cleanest fork in the study. All 6: show-greyed-with-named-shortfall; reject omit AND show-normally. Realized positives not target labels; UNDER_SAMPLED/CORPUS_LIMITED/EMPTY distinction; structurally non-strippable. → resolves **DF-3 (Open Items 3,5)**; confirms **UC-16**, **UC-23 (d)**.

### DF-4 · Real-data correlation slice — **RESOLVED: the named credibility unlock; use synthetic now WITH the ceiling caveat, do NOT block v0.1 on it.**
**Unanimous** that the **real-corpus correlation study (vs i2b2-2014 / TAB; cycle-1 UC-13)** is the single highest-leverage upgrade — and **unanimous** that they'd **use the synthetic numbers now** with an explicit ceiling caveat rather than wait. The persona-specific framing of *what the study unlocks*:
- INT-01 (clinical): upgrades a **supplementary stress-test** to a **primary result** ("Rankings track i2b2-2014, Spearman ρ=… [CI]" is "publishable as a primary result at ACL or JAMIA"). The synthetic-only ceiling is **the dominant anxiety** and "the single biggest reason a skeptical reviewer could dismiss results."
- INT-02 (methods): upgrades an ablation-grade claim to a generalization claim; would wait for a primary comparison paper but not for an ablation.
- INT-03 (tool-builder): upgrades **"CI gate only"** to **"citable model-card score"** at **Kendall-τ > 0.8**.
- INT-04 (priv-eng): the unlock is **stakeholder-facing** — moves "we think these numbers are predictive" to "shown to track real-data behavior" for security leadership; he flags that **powered + synthetic is genuinely harder to sell** than powered alone, so the correlation study must at least be **named-and-pending** in the report.
- INT-05 (assurance): upgrades **"evidence scaffolding"** to **"supporting evidence for a real-world capability claim"**; without it the ceiling statement must read "these results do not constitute evidence of real-world re-identification resistance."
- INT-06 (red-team): the study tells him **how much to trust the oracle floor** (synthetic recall vs real-world adversarial recall); discounts synthetic by an unknown factor until then.

**Resolution:** carry **cycle-1 UC-13 real-data correlation** as the **top credibility unlock** to Requirements + Testing Pass-2, **out-of-band, NOT v0.1-blocking** (gated on external DUA). The **non-strippable synthetic-only caveat (C-6)** is the load-bearing v0.1 substitute, and INT-04's **"correlation study named-and-pending" flag** is a `→ R4 (report requirement)`. → confirms **Open Item 11**; sharpens **UC-23**, **C3** (the ceiling-statement template).

### DF-5 · Leaderboard hygiene / anti-gaming — **RESOLVED: blind submission + held-out-label separation + provenance stamp + contamination disclosure; arms-length governance is a hard gate.**
Converged across the personas whose threat-model justifies it (INT-02 consumer-side, INT-03 submitter-side, INT-05 assurance-side, INT-06 oracle-side):
- **Contamination is the hardest dismissal to disarm** (INT-03; the corpus is public synthetic → train-on-test). Mechanism: **per-system `contamination_status`** with **"unknown" rejected/flagged**, plus a **signed attestation that held-out labels were not exposed** (INT-03's 6th AX-005 element). INT-02: a leaderboard that accepts `contamination_status: unknown` "is not trustworthy."
- **Blind submission** (evaluator runs inference, not submitter; held-out labels genuinely held out) — INT-03 would accept signing an attestation rather than open-sourcing weights; the **vendor-gaming sub-archetype's own incentive** (avoid disclosing training data / held-out set) is the forcing function INT-05 names as the **structural P-tool-vendor / P-complreviewer tension** the governance must resolve explicitly.
- **Arms-length / neutral governance is inadmissibility-grade** for the assurance consumer: INT-05 "rejected vendor self-benchmarks as inadmissible on two recent mandates"; "a tool vendor running their own benchmark is inadmissible regardless of methodology quality." INT-04 (procurement): vendor self-benchmarks are "inadmissible to my procurement process." Both want a **named neutrality/governance statement** (who controls the corpus, who holds the labels, who runs the eval) **as a field in the run-record + every report page**, not a general caveat. INT-05 adds: a managed conflict (academic group with a system in the leaderboard) needs **explicit disclosure + a recusal record**.

→ resolves **DF-5 (Open Items 8,9,10)**; confirms **UC-17 (d)**, **UC-23**; `→ R4 (governance NFRs: blind/provenance submission, contamination disclosure, neutrality statement field, recusal record)`.

---

## 4. Disagreements & tensions (→ R6/R7 to adjudicate; Pass-2 to validate)

1. **DF-1 default mode (the only real fork-level disagreement).** Powered-sample-default (4 operational voices) vs full-corpus-default-for-filing (INT-05). **Adjudication:** keep the locked default; make the citable-vs-pre-screen distinction first-class via the **run-type designation**; carry INT-05's filing-grade-full-corpus pushback to R6/R7 + Pass-2. (§3 DF-1)

2. **AX-pii-anon-005 universality vs run-type scoping.** INT-02 wants the bar (CI + paired test + convergence + provenance + caveat) **enforced unconditionally, including exploratory runs** ("the exploratory run is where people get sloppy and then report the sloppy number"). INT-05 wants it **enforced only on filing-grade designation**, warning universal enforcement makes teams "avoid the formal mode and file development outputs instead." INT-03/INT-06 sit between (relax convergence + paired-test for internal gates; full bar for published/leaderboard). **Adjudication:** these are reconcilable via the **run-type mechanism** — the *full* AX-005 bar binds the published/leaderboard/filing run-types (satisfies INT-05), while smoke/dev runs **suppress** CI/p-value emission entirely (UC-20) rather than emit a sloppy number (satisfies INT-02's underlying worry — there is no un-rigorous number to mis-cite because there is no number). Confirm the exact per-run-type bar in **R6/R7**. (§5)

3. **AX-004 salience is persona-stratified, not universal.** Gating for INT-05 (legal end-states) and valued by INT-02/03 (methods); **low-salience for INT-01** (clinical de-id = irreversible removal, no pseudonymization reasoning) and not relevant to INT-06. **Adjudication:** keep AX-004 MUST (it is architecturally load-bearing and a binding axiom), **document the audience**; do not let the clinical lens's indifference weaken it. Mirrors the cycle-1 finding that pseudonymization value is persona-stratified, not OUT.

4. **Scope boundaries — accepted, but each MEDIUM persona wants its own boundary named.** All accepted the honest bounds, but each surfaced a **different out-of-scope item they care about**: INT-06 → **re-id power** out is fine ("detection/anonymization power only, re-id is cycle-2" — MEI-06 accepted) **and** the **adversarial-distribution gap** (homoglyph/BiDi/encoding) "no statistical powering fixes" — wants a **coverage envelope** naming which (entity-type × language) cells are powered vs `NOT_ASSESSED`; INT-04 → **re-id / oracle API out** fine, but **regulatory taxonomy crosswalk** must be **in** (see #5 below); INT-05 → **re-id out** fine, but **anon/pseudo separation** and **Art-11 format** must be in. **Adjudication:** the boundaries hold (UC scope-boundary statements + MEI-06); the **net-new in-scope asks** are routed individually in §5. The **coverage envelope / NOT_ASSESSED** ask (INT-06) reinforces **UC-16 DOM-03** and should be surfaced as a **first-class artifact consumed by oracle callers**, not just a reporter artifact.

5. **Citation-format gate (DOI) is HARD for the academic cohort, silent for others.** INT-01 makes a **Zenodo DOI a hard gate** (won't cite a SHA in JAMIA/ACL); INT-02 confirms; INT-03/04/05/06 didn't raise it (CI-gate / procurement / oracle / filing uses don't need a DOI). **Adjudication:** **C2 is MUST-level for the P-acad-deid citation cohort**, SHOULD elsewhere — carry as a persona-stratified priority, not a universal MUST. → R6/R7.

6. **Throughput SLA is a HARD pre-gate for INT-06, absent for everyone else.** INT-06: "latency and throughput … the first thing I'd check before wiring anything in"; **≤50 ms p95 / ≥500 rps** is a gate *before* any methodological evaluation. The HIGH cohort never raised it (detection is compute-cheap; they don't call an inline oracle). **Adjudication:** route to the **oracle track (cycle-1 UC-08 / C9)**, make `benchmark_throughput.py` output machine-readable in **UC-23**; **not a CAP-02-v0.1 blocker** but a named MEDIUM-persona requirement. → R6/R7.

**No interviewee challenged the core spine** (`load → sample → run → score → rate → report`), the **OWNS/CONSUMES boundary**, or the **engine-reuse posture** — in fact INT-01 (G1/G6 probe) said reuse "makes me **substantially more** likely to trust and cite it … Integration of established machinery is the honest framing." The disagreements are all at the **scoping/priority** layer, not the architecture layer.

---

## 5. How this sharpens the FR/NFR set (handoff to R4)

The R3 signal sharpens the R1 capability set in **seven** ways. (FR/NFR IDs deliberately omitted — R4 mints them from offsets FR→030 / NFR→019.)

1. **Run-type designation is a NET-NEW first-class capability** (no R1 home). It is the mechanism that resolves **DF-2** (pre-reg scoping) AND the **AX-005 universality tension** (§4 #2) AND the **DF-1 citable-vs-pre-screen distinction** (§3). R4 should author an FR: a declared run-type (`smoke` / `dev` / `leaderboard-submission` / `filing-grade`, names TBD) that selects the enforced rigor bar, the backing sample mode, and the pre-registration gate. This is the single highest-leverage new requirement R3 surfaced. `→ R4 (new FR)`

2. **AX-pii-anon-005 should be PROPOSED with two sharpenings the cohort demanded.** Record it in the requirements doc for confirmation, wire NFRs to it, and add: (a) **multiplicity-family declaration is explicit in the axiom text** (exploratory per-cell uncorrected vs confirmatory Holm-Bonferroni **with family size**) — INT-01/02, "reviewers will ask"; (b) **a 6th element: contamination disclosure / signed held-out-non-exposure attestation** for leaderboard entries — INT-03. The axiom's five-element core (CI + paired test + convergence + provenance + synthetic-only caveat) is confirmed by all who reached probe #17 as **"the right bar."** Its **enforcement scope** is run-type-bound (§4 #2). The convergence element should specify **achieved max-RD ± 2RD + rounds stated** (INT-02; G4).

3. **Three NET-NEW capabilities come ONLY from the MEDIUM personas** — the most valuable MEDIUM contribution, and they would have been missed without the DPO sampling-gap closure + the red-team + priv-eng sections:
   - **Operating-point reporting** (INT-04 + INT-03): recall-priority Fβ / FN:FP cost + AUPRC at the pre-registered operating point; the **false-positive tax** (34k FPs / 3× license cost) is the quantified "why." Sharpens **UC-23 (a)**, **DOM-02**. `→ R4 (FR + NFR)`
   - **Filing-grade artifact format** (INT-05 + INT-04): per-metric inline CI + provenance hash + caveat; **+ named Art-11 alignment** (which fields satisfy EU AI Act Art-10/11, "a time-sensitive hard requirement … clients need … before August" 2026); + **file-level provenance** (C5). Sharpens **UC-22/23**, **C3/C7**. `→ R4 (FR + NFR)`
   - **Oracle throughput SLA + callable-oracle surface** (INT-06): ≤50 ms p95 / ≥500 rps, machine-readable, + **contamination_status propagated into the oracle API** + **coverage envelope as a caller-consumed artifact**. Routes to **C9 / cycle-1 UC-08**; make `benchmark_throughput.py` output machine-readable in **UC-23**. `→ R4 (NFR, oracle track)`

4. **Regulatory taxonomy crosswalk — author it, but make it auditable/updateable (resolves the Open-Item-10 author-vs-defer fork).** INT-04 and INT-05 **both** need a **63-entity-type → regulatory-regime mapping** (GDPR Art-9 special categories / HIPAA PHI / CCPA / GLBA). INT-05 (the probe that owned the fork): "**author it, but make it auditable and updateable**" — he'll use a workflow-provided crosswalk with caveats rather than rebuild it; INT-04 builds it manually every cycle and calls it "**more load-bearing than the pre-registration question**." **Resolution: AUTHOR (do not defer)**, with an auditable/updateable mapping artifact. Sharpens **UC-23**, **Open Item 10**, **AX-004 crosswalk**. `→ R4 (FR)`

5. **Several R1 items must move from CODE-enforced to ALSO DOC/REPORT-enforced.** The cohort repeatedly drew a line between *enforced by construction* (necessary) and *explained in prose* (also necessary, because reviewers/regulators ask and the author must answer without reading source): (a) **AX-004 separation rationale** (INT-01/05); (b) **multiplicity-family rationale** (INT-01); (c) **LONG_TAIL / NIST §7.2.4.2 power-tier derivation visible in/alongside the report** (INT-01, C10 — "the single best defense against the 'arbitrary thresholds' objection"); (d) **academic-vocabulary register** — no product "SHIP-WITH-CAVEATS" in a manuscript (INT-01, C7). `→ R4 (report/doc requirements wired to UC-23 + C7/C10)`

6. **Hygiene/governance NFRs are confirmed and quantified** (DF-5, §3): contamination disclosure with "unknown" rejected; blind submission + held-out-label separation; signed attestation; **neutrality/governance statement as a structured run-record + per-report field**; recusal record for managed conflicts. The **arms-length requirement is inadmissibility-grade** for both INT-04 and INT-05. `→ R4 (governance NFRs)`

7. **Reproducibility/correctness NFRs gain teeth from confirmed burns.** The dataset-seam regression contract (C-2) and the audited-stats quarantine (C-1) are no longer abstract — two interviewees lost **multi-week-to-multi-month** effort to silent drift, and three demanded **CI-enforcement, not documentation**. R4 should make the **quarantine assertion** (audited path always taken, fabricated path unreachable) and the **regression contract** (incl. content hash + run-record field + delta attribution) **testable NFRs**, not prose. `→ R4 (NFRs with verification methods)`

---

## 6. Net-new requirement candidates (consolidated for R4 — descriptive, un-numbered)

| # | Candidate capability/attribute | Source(s) | Anchors |
|---|---|---|---|
| N-01 | **Run-type designation** scoping pre-reg + AX-005 bar + backing sample mode (highest-leverage new item) | INT-01/02/03/05/06 | DF-1/DF-2 · UC-18/19 |
| N-02 | **AX-pii-anon-005** proposed + recorded for confirmation; +multiplicity-family in axiom text; +6th contamination element; run-type-scoped enforcement | INT-01/02/03 | §5.2 |
| N-03 | **Operating-point reporting** (Fβ β>1 / FN:FP cost + AUPRC @ pre-reg operating point) | INT-04, INT-03 | UC-23 · DOM-02 |
| N-04 | **Filing-grade artifact format** (per-metric inline CI+hash+caveat) + **Art-11 alignment label** + **file-level provenance** | INT-05, INT-04 | UC-22/23 · C3/C5/C7 |
| N-05 | **Regulatory taxonomy crosswalk** (63 types → GDPR/HIPAA/CCPA/GLBA), auditable/updateable — **AUTHOR not defer** | INT-04, INT-05 | Open Item 10 · AX-004 |
| N-06 | **Oracle throughput SLA + callable surface** (≤50 ms p95 / ≥500 rps, machine-readable; contamination flag in API; coverage envelope caller-consumed) | INT-06 | C9 · cycle-1 UC-08 |
| N-07 | **CI-enforced stats-path quarantine** (audited path always taken; fabricated `significance.py` unreachable from run surface) | INT-02, INT-03 | UC-17 · Open Item 1 |
| N-08 | **Regression-contract as run-record field** + content-hash (anti same-count swap) + **corpus-vs-model delta attribution** | INT-02, INT-03 | UC-21 · C1 |
| N-09 | **Rank-stability (Kendall-τ) + max-RD±2RD as first-class output fields** (≥2–3 seeds) | INT-02, INT-03 | UC-17/23 |
| N-10 | **Smoke reference-output schema spec** (per-type F1±tol, byte-diffable, CI/p suppressed, <10 min) | INT-03, INT-02 | UC-20 · C4 |
| N-11 | **Contamination-status hygiene**: "unknown" rejected/flagged + signed held-out-non-exposure attestation + flag propagation | INT-02, INT-03, INT-06 | DF-5 · UC-17/23 |
| N-12 | **Neutrality/governance statement** as structured field (corpus owner / label holder / evaluator) + recusal record | INT-04, INT-05 | DF-5 · Open Items 9,10 |
| N-13 | **DOI / Zenodo persistent identifier** — MUST for academic-citation cohort, SHOULD elsewhere | INT-01, INT-02 | C2 |
| N-14 | **Report-prose requirements**: AX-004 rationale, multiplicity rationale, LONG_TAIL/NIST derivation visible, academic vocabulary | INT-01, INT-05 | C7/C10 · UC-23 |
| N-15 | **Correlation-study "named-and-pending" flag** in the report (until cycle-1 UC-13 lands) | INT-04, INT-05 | DF-4 · Open Item 11 |

---

## 7. Methodology & Epistemic Honesty

- **6 agent-simulated interviewees** (INT-01…INT-06), one per persona, dispatched against the R2 guide (18 core + Layer-2 + sub-archetypes). INT-05 carried the **P-complreviewer** sub-archetype (closing the documented CM-05 / un-sampled-DPO gap from R2); INT-03 carried the **P-tool-vendor** gaming-incentive angle. **Representative-scale, single-session, AGENT_SIMULATED** — directional, not confirmatory or saturated.
- **Every quantified threshold in §2 is agent-simulated and illustrative.** Each interviewee flagged its own numbers (McNemar p=0.14, Spearman ρ=0.91, Kendall-τ>0.8, 50 ms/500 rps, 34k FPs/3× license, <10 min smoke) as needing **real-user verification in Pass-2**. They are the directional thresholds the NFR-author proposes and the threshold-validator (R9) pressure-tests — not committed values.
- **Two inherited structural caveats** (R2): (1) **DPO/assurance signal is FIRST CONTACT** — a clean INT-05 result does **not** re-confirm the cycle-1 P-dpo priority; real-user DPO interviews remain a Pass-2 must. (2) The **real-data correlation slice** (cycle-1 UC-13, vs i2b2-2014/TAB) is the named citation unlock and remains **out-of-band / not v0.1-blocking**.
- **ID discipline honored**: no FR/NFR IDs minted here; the interviewees' clashing `FR-030…/NFR-019…` numbers were **deliberately not canonicalized** (R4's job). Synthesis anchored to stable UC-16…23, DF-1…5, AX-001/002/003/004/005-candidate, C1…10, Open Items 1–13, G1–G7.
- **Disconfirmation worked**: the mandatory rejection probes surfaced the **three walk-away triggers** (fabricated stats / unpinned seam / missing power verdict — named by multiple personas) and the **mandatory-pre-reg avoidance risk** (4 personas) — i.e., the guide elicited rejection signal, not just assent, satisfying the R2 confirmation-bias control.
- **Sources read this session (canonical):** `01-discovery/discovery-report.md` (§8 Open Items 1–13, G1–G7, C1–C10), `01-discovery/04-use-cases.md` (UC-16…23 + acceptance signals), `01-discovery/personas.md` (6 personas + 2 sub-archetypes), `02-requirements/interview-guide.md` (DF-1…5 ownership, AX-005 probe, self-bias-check), `02-requirements/interview-synthesis.md` cycle-1 (format reference). Both repos available for threshold grounding.

✅ **R3 simulated-interview synthesis complete (2026-06-01).** 6 interviews synthesized; **DF-3 resolved (zero dissent), DF-2/DF-4/DF-5 resolved, DF-1 split-by-use (locked default holds + run-type distinction; INT-05 filing-grade pushback carried to R6/R7+Pass-2)**; **AX-pii-anon-005 confirmed as the right bar with 2 sharpenings (multiplicity-family-in-text + 6th contamination element) and run-type-scoped enforcement**; **15 net-new requirement candidates** consolidated (3 from MEDIUM personas only). `provisional_status: AGENT_SIMULATED`; DPO lens first-contact; real-data slice + real-user validation are Pass-2. Ready for R4 (FR/NFR authoring; offsets FR→030, NFR→019).
