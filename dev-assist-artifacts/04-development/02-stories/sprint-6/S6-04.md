# S6-04 — GOVERNANCE.md: charter + advisory roster + CoI + bus-factor (FR-026 / NFR-014)

> **Cold-pickup**: self-contained; §8b authoritative. **Test names carry `fr_026`/`nfr_014` tokens.** A
> `GOVERNANCE.md` charter naming `pii-anon-core`, an advisory-body roster (flagged **AGENT_SIMULATED** —
> aspirational, not yet real people), a CoI statement (referencing the S6-03 recusal control), and an
> **honest bus-factor = 1** succession note. Pinned by `tests/test_governance.py`.

## 1. Metadata

| Field | Value |
|---|---|
| **Parent epic** | S6 — Leaderboard & governance (DC-13) |
| **Sprint** | S-6 (Leaderboard + governance) |
| **Status** | DONE |
| **Estimated agent-effort** | S (≤1d) |
| **Owner agent** | orchestrator (doc story — authored directly, RED→GREEN) |
| **Story gate** | APPROVE (iter 1, 5/5 reviewers, combined S6-04-05; `_reviews/story/S6-04-05/synthesis.md`) |
| **provisional_status** | AGENT_SIMULATED |

## 2. Goal

Author `GOVERNANCE.md` (FR-026 / NFR-014): the project's governance charter. It MUST contain a **charter**
(mission + decision process), an **advisory-body roster** explicitly flagged **AGENT_SIMULATED** /
aspirational (the project does not yet have real advisory members — honesty over optics), a **conflict-of-
interest statement naming `pii-anon-core`** (the maintainer affiliation + the recusal policy implemented
by the S6-03 `CoIRecord` / S6-01 store), and an **honest bus-factor = 1** succession note. Pinned by a new
`tests/test_governance.py` so the governance artifact cannot silently disappear (NFR-014 governance
checklist).

## 3. Traced requirements

**Primary**: FR-026 (governance charter — GOVERNANCE.md + advisory-body roster + CoI statement re:
`pii-anon-core` + bus-factor/succession note). **Supporting**: NFR-014 (governance neutrality / auditable —
GOVERNANCE.md + roster + CoI present), epistemic honesty (the aspirational roster is flagged
AGENT_SIMULATED; bus-factor=1 stated plainly).

## 4. Source DC + provisional_status

DC-13 (leaderboard & governance) · AGENT_SIMULATED · not DIVERGED. The advisory roster is aspirational
(no real members yet) — flagged AGENT_SIMULATED in the doc itself.

## 5. Dependencies

**Must be DONE**: S6-01 (store), S6-03 (CoIRecord) — GOVERNANCE.md references the held-out store + recusal
control. ✅
**Blocks**: S6 sprint close (FR-026 charter half).

## 6. Test-first plan (RED→GREEN) — `fr_026`/`nfr_014` tokened

`tests/test_governance.py`:
1. `test_fr_026_governance_md_has_charter_and_roster` — `GOVERNANCE.md` exists and contains a charter
   (mission/decision-process) AND an advisory-body roster section that is explicitly flagged
   `AGENT_SIMULATED` / aspirational — `[PROPERTY-TEST]`.
2. `test_fr_026_governance_md_has_coi_and_bus_factor` — `GOVERNANCE.md` contains a CoI statement naming
   `pii-anon-core` (and referencing recusal) AND an honest bus-factor note (`bus-factor` / `bus factor` +
   the number 1 / "single maintainer") — `[PROPERTY-TEST]`.
3. `test_nfr_014_governance_md_present` — `GOVERNANCE.md` exists and the governance checklist holds:
   GOVERNANCE.md + advisory roster + CoI statement all present (NFR-014) — `[PROPERTY-TEST]`.

## 7. Acceptance criteria (G/W/T from FR-026 / NFR-014)

> **Given** the project, **When** GOVERNANCE.md is published, **Then** it contains a charter, an advisory
> roster (flagged AGENT_SIMULATED/aspirational), a CoI statement naming `pii-anon-core` (with recusal), and
> an honest bus-factor=1 succession note — pinned by `tests/test_governance.py`.
> **Negative**: the roster is NOT presented as a real, staffed body (it is flagged aspirational).

## 8. Files this story owns

**NEW**: `GOVERNANCE.md`, `tests/test_governance.py`

## 8b. Implementation spec (authoritative — required GOVERNANCE.md sections)

GOVERNANCE.md MUST include (the test asserts the load-bearing strings):
- **# Governance** title + a **charter / mission** (what PII-Anon is, the neutrality goal) + a
  **decision-making process** (how changes/releases are decided).
- **## Advisory body** — a roster section that OPENLY states it is **aspirational / AGENT_SIMULATED**: the
  project does not yet have a staffed advisory board; this lists the roles it intends to fill. (Honesty:
  do NOT fabricate named members.)
- **## Conflict of interest** — names **`pii-anon-core`** as the maintaining affiliation; states the
  recusal policy: maintainer / `pii-anon-core`-affiliated submitters are flagged for recusal from
  self-adjudicating their own leaderboard entry (implemented by `leaderboard.CoIRecord.requires_recusal`
  + the append-only held-out store); the held-out gold is never distributed.
- **## Bus factor & succession** — an HONEST note that the current **bus factor = 1** (single maintainer),
  with the intended succession path (advisory body + CC0 data + Apache-2.0 code mean the project can be
  forked/continued by anyone).
- **## Neutrality controls** — points to the leaderboard governance seam (held-out store with
  `verify_chain`, opt-in publish, anti-gaming policy, CoI recusal).

**Load-bearing invariants:** (1) **FR-026 completeness** — charter + roster + CoI(pii-anon-core) +
bus-factor all present (the test pins each). (2) **honesty (NFR-014 / epistemic)** — the roster is flagged
aspirational/AGENT_SIMULATED (no fabricated members); bus-factor=1 stated plainly. (3) the CoI statement
ties to the S6-03 recusal control.

## 9. Test type tags

`[PROPERTY-TEST]`

## 10. Definition of Done

- [x] RED→GREEN (3 `fr_026`/`nfr_014`-tokened tests; RED — GOVERNANCE.md absent — precedes the GREEN doc).
- [x] GOVERNANCE.md has charter + aspirational(AGENT_SIMULATED) roster + CoI(pii-anon-core, recusal) +
      bus-factor=1; honesty held (no fabricated advisory members).
- [x] full suite 0 regressions (319 → ~322 passed, 1 pre-existing skip).

## 11. Reviewer agents at story gate

code-quality · traceability · requirements-coverage · security-sast · axiom-compliance.
(axiom-compliance load-bearing: epistemic honesty — the aspirational roster flag + the plain bus-factor=1;
requirements-coverage: FR-026 charter completeness.)

## 12. Evidence on completion

**provisional_status: AGENT_SIMULATED.** Authored directly (orchestrator, strict RED→GREEN); the
governance roster is aspirational/AGENT_SIMULATED by design (no real members).

**TDD:** RED `a48f407` (`tests/test_governance.py` — 3 `fr_026`/`nfr_014` tests fail, GOVERNANCE.md absent)
→ GREEN `64ae813` (`GOVERNANCE.md`). 3/3 GREEN; full suite **322 passed / 1 skipped** (319 prior + 3).

**The 3 tests** (all `fr_026`/`nfr_014`-tokened): `test_fr_026_governance_md_has_charter_and_roster`
(charter + aspirational AGENT_SIMULATED roster), `test_fr_026_governance_md_has_coi_and_bus_factor` (CoI
naming pii-anon-core + recusal + bus-factor=1), `test_nfr_014_governance_md_present` (governance checklist:
GOVERNANCE.md + advisory + CoI present).

**GOVERNANCE.md section audit:** charter & mission + decision process; **## Advisory body** flagged
ASPIRATIONAL / AGENT_SIMULATED (roles-only, zero fabricated names); **## Conflict of interest** naming
`pii-anon-core` + the `CoIRecord.requires_recusal` recusal control + held-out-gold-never-stored; **## Bus
factor & succession** = 1 / single maintainer (honest), CC0/Apache-2.0 fork path; **## Neutrality
controls** mapping to the real leaderboard seam (`verify_chain`, opt-in publish, anti-gaming, recusal).

### Reviewer verdicts

Combined S6-04+S6-05 story gate **APPROVE** (iteration 1, 5/5 reviewers; `_reviews/story/S6-04-05/synthesis.md`)
— zero SHOWSTOPPER/CATASTROPHIC/MAJOR; OBSERVATIONs only (axiom-compliance confirmed the aspirational-roster
honesty + the CoI claims are true-of-code). **FR-026 + NFR-014 verifiable-DONE at the S6 sprint gate.**
