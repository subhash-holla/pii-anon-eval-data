# S5-07 — Documentation-drift remediation (NFR-013)

> **Cold-pickup**: self-contained; §8b is the authoritative per-file edit map. **Test names carry the
> `nfr_013` token.** Brings 7 docs to the canonical counts so record-count / entity-type-count / version
> are identical across README / DATASHEET / COMPARISON / TAXONOMY / MIGRATION / CHANGELOG / `__init__`
> (the NFR-013 measure). A new `tests/test_doc_drift.py` makes the drift continuously enforced (RED →
> GREEN). Verified by `/dev-assist-validate` → 0 drift.

## 1. Metadata

| Field | Value |
|---|---|
| **Parent epic** | S5-C — Doc-drift (NFR-013) |
| **Sprint** | S-5 (Exports/CLI + Compliance + doc-drift) |
| **Status** | DONE |
| **Estimated agent-effort** | M (≤3d) |
| **Owner agent** | dev-assist-development-executor (claimed 2026-05-30; IN_PROGRESS on RED; REVIEW 2026-05-30) |
| **Story gate** | APPROVE (iter 1, 5/5 reviewers; `_reviews/story/S5-07/synthesis.md`) |
| **provisional_status** | AGENT_SIMULATED |

## 2. Goal

Close NFR-013 (documentation currency — zero drift): the record count, annotation count, entity-type
count, and version MUST be identical across all docs. Today they are NOT (DATASHEET still says
`117,752 / 919,000+ / 57 types / v1.1.0`; COMPARISON says `160K / 65`; README's stats say `159,891 /
~1.24M / 65` and mis-frames the 159,891 as "100%"; `__init__.py` says "150K+ / 65"; MIGRATION has no
v1.3.0→v2.0.0 section; TAXONOMY's body still lists ~80 rows). Remediate all 7 to **canonical**, add the
missing sections, and pin a `test_doc_drift.py` so the drift cannot recur.

**CANONICAL TRUTH** (from `data/pii_anon.metadata.json` + `taxonomy.ENTITY_TYPE_COUNT`):

| Field | Value |
|---|---|
| version | **2.0.0** |
| total_records | **575,604** |
| total_annotations | **2,486,438** |
| entity_types | **63** (== `taxonomy.ENTITY_TYPE_COUNT`) |
| entity_categories | **9** |
| languages | **60** |
| evaluation_dimensions | **7** |
| tier3_evaluation_records | **159,891** = the **~27.8% EVALUATION substrate** (NOT "100%") |
| enrichment | **~72%** of records carry `provenance.source_type="synthetic_lattice_enrichment"` (S-PWR) |

**Per-category entity counts (the 63, from `taxonomy.ENTITY_REGISTRY`):** contact **2** · digital_online
**10** · employment **4** · financial **12** · government_legal **10** · identity_demographics **6** ·
location_temporal **6** · medical_biological **8** · special_category **5** (= 63).

**S-PWR enrichment facts:** corpus grew **159,891 → 575,604 records** (+415,713) and annotations →
**2,486,438** (+1,246,801); all 710 committed lattice cells powered; NFR-018 power gate PASS; tags
`v1.3.0` + `pre-lattice-enrichment` pinned.

**Train-vs-eval note (add where the 159,891 appears):** "The 159,891 `tier3_evaluation` records are the
~27.8% EVALUATION substrate of the 575,604-record corpus — behavioral-signal / RRS scoring runs on this
substrate; the full corpus is the detection substrate."

**§7 authoritative power sentence (add to README + DATASHEET, verbatim):**
> "PII-Anon v2 is powered for all single-factor marginal recall claims (95% Wilson CIs; credential/
> financial-critical types to ±0.5pp at recall 0.99, standard to ±1pp at 0.98) and for three
> pre-registered 2-way interactions (language×entity-type on a 12×41 committed rectangle, domain×track,
> adversarial-type×entity-type). It is not powered for the full multilingual×entity-type grid or any
> ≥3-way interaction; those are reported as exploratory. Synthetic-distribution power is not external
> validity — see the real-data correlation slice."

## 3. Traced requirements

**Primary**: NFR-013 (documentation currency — record/entity-type/version identical across docs; zero
drift; Datasheet complete). **Supporting**: M1 (single-source-of-truth entity count = 63), AX-002
(deterministic doc-test), epistemic honesty (the ~72% formulaic-enrichment + synthetic≠external-validity
caveats are surfaced, not hidden).

## 4. Source DC + provisional_status

DC-02 (schema; NFR-013 reconciliation) · AGENT_SIMULATED · not DIVERGED.

## 5. Dependencies

**Must be DONE**: S5-03 (the generated dataset card already pulls canonical counts — the static docs must
now agree). ✅
**Blocks**: S5 sprint close (`/dev-assist-validate` 0-drift is a sprint-gate input).

## 6. Test-first plan (RED→GREEN) — `nfr_013` tokened

`tests/test_doc_drift.py` (reads the repo docs; the canonical constants come from
`taxonomy.ENTITY_TYPE_COUNT` + a small literal set — never hardcode 63 where the registry can supply it):
1. `test_nfr_013_docs_use_canonical_record_count` — README.md, DATASHEET.md, COMPARISON.md each contain
   `"575,604"` and contain NONE of the stale strings `"117,752"`, `"919,000"`, `"150K+ records"`,
   `"~1.24M"`, `"1.24M"` — `[PROPERTY-TEST]`.
2. `test_nfr_013_docs_use_canonical_entity_count` — README.md, DATASHEET.md, COMPARISON.md,
   `src/pii_anon_datasets/__init__.py` reflect **63** entity types (== `taxonomy.ENTITY_TYPE_COUNT`) and
   contain NONE of the **current-state** stale claims `"65 entity types"`, `"65 types"`, `"57 entity
   types"`, `"57 types"` — `[PROPERTY-TEST]`.
3. `test_nfr_013_doc_titles_are_v2` — DATASHEET.md and COMPARISON.md titles/headers declare **v2.0.0**
   (not `v1.1.0` / `v1.3.0`) — `[PROPERTY-TEST]`.
4. `test_nfr_013_taxonomy_body_matches_registry` — every UPPER_SNAKE entity-type token in TAXONOMY.md's
   canonical table is a member of `taxonomy.ENTITY_REGISTRY`, AND all 63 registry types appear in
   TAXONOMY.md (no orphan/extra canonical rows; the body is pruned to exactly the 63) — `[PROPERTY-TEST]`.
5. `test_nfr_013_migration_has_v2_section` — MIGRATION.md contains a `v1.3.0 → v2.0.0` (or `→ 2.0.0`)
   section that mentions `tier3_evaluation`, `record_id` (content-addressed), and
   `compat.to_v1_record` — `[PROPERTY-TEST]`.
6. `test_nfr_013_changelog_has_spwr_entry` — CHANGELOG.md contains an S-PWR / power-enrichment entry
   under 2.0.0 mentioning `575,604` and `synthetic_lattice_enrichment` — `[PROPERTY-TEST]`.

## 7. Acceptance criteria (G/W/T from NFR-013)

> **Given** the 7 project docs,
> **When** the remediation is applied,
> **Then** the record count (575,604), annotation count (2,486,438), entity-type count (63), and version
> (2.0.0) are identical across README / DATASHEET / COMPARISON / TAXONOMY / `__init__`, the train-vs-eval
> + ~72%-enrichment + §7-power caveats are surfaced, MIGRATION has a v1.3.0→v2.0.0 section, CHANGELOG has
> an S-PWR entry, and TAXONOMY's body is exactly the 63 registry types — and `/dev-assist-validate`
> reports **0 drift**.
> **Negative**: no current-state doc contains a stale `150K/160K/117,752/919,000/~1.24M/65/57` count or a
> `159,891 (100%)` claim (historical CHANGELOG version entries are preserved as-is — they record what
> each prior version claimed).

## 8. Files this story owns

**NEW**: `tests/test_doc_drift.py`
**MODIFIED**: `README.md`, `DATASHEET.md`, `COMPARISON.md`, `TAXONOMY.md`, `MIGRATION.md`, `CHANGELOG.md`,
`src/pii_anon_datasets/__init__.py`.
**PRESERVE**: the CHANGELOG's HISTORICAL version entries (v1.0/1.1/1.2/1.3) — they are a record of what
each version claimed; only ADD the S-PWR sub-entry under 2.0.0. Do NOT rewrite history.

## 8b. Implementation spec (authoritative per-file edit map)

Apply canonical values; surface the caveats; do not touch the corpus/lattice/tags. Historical changelog
entries stay verbatim.

**`README.md`**
- L3: `…159,891 high-quality synthetic records` → `…575,604 synthetic records (159,891 of which form the
  Tier-3 re-identification EVALUATION substrate)`. (60 languages / 63 entity types / 7 dims already correct.)
- L16 `Total Records | 159,891` → `575,604`; L17 `Total Annotations | ~1.24M` → `2,486,438`.
- L25 `Behavioral Signal Annotations | 159,891 (100%)` → `159,891 (27.8% — the Tier-3 evaluation
  substrate)`; L28 `Per-record RRS Score | 159,891 (100%)` → `159,891 (27.8% — Tier-3 evaluation substrate)`.
- L250 `…all 65 entity types` → `…all 63 entity types`; L258 `Entity Categories (9 categories, 65 types)`
  → `(9 categories, 63 types)`; L340 `65 entity types` → `63 entity types`; L356 `159K records, 65 entity
  types` → `575,604 records, 63 entity types`.
- ADD a stats-table row `| **Synthetic-lattice enrichment (S-PWR)** | ~72% of records
  (provenance.source_type=synthetic_lattice_enrichment) |` and, near the stats, the **train-vs-eval note**
  + the **§7 power sentence** (both verbatim from §2).

**`src/pii_anon_datasets/__init__.py`**
- L4: `150K+ records across 60 languages, 65 entity types, 20+ document formats` → `575,604 records
  across 60 languages, 63 entity types (canonical: taxonomy.ENTITY_TYPE_COUNT), 40 document formats`.

**`DATASHEET.md`**
- L1 `…v1.1.0` → `…v2.0.0`.
- L22 `117,752 records containing 919,000+ annotations across 57 entity types` → `575,604 records
  containing 2,486,438 annotations across 63 entity types`.
- L38 `entity type (57 types)` → `entity type (63 types)`.
- ADD an intended-use **train-vs-eval note** (the 159,891 = 27.8% eval substrate) + the **~72%
  synthetic_lattice_enrichment** disclosure + the **§7 power sentence**; add `v2.0.0: May 30, 2026` to the
  version-history line.

**`COMPARISON.md`**
- L3 `…v1.3.0` → `…v2.0.0`; L9/L17 `PII-Anon v1.3` → `PII-Anon v2.0`.
- L19 PII-Anon `Records | 160K` → `575,604`; L22 PII-Anon `Entity types | 65` → `63`.
- L25 `All 160K records, 6 categories` → `159,891 Tier-3 records (27.8% eval substrate), 6 categories`;
  L26 `All 160K records` → `159,891 Tier-3 records`.
- L52/L67/L72/L77 `57 entity types` → `63 entity types`; L90 `150K records` → `575,604 records`;
  L99/L111 bar-chart `PII-Anon v1.2 … 151K/…` → `PII-Anon v2.0 … 576K` (keep the relative bar lengths).
- **Competitor corrections (apply the plan-verified deltas):** Nemotron `200K` → `~100K`; AI4Privacy
  `580K` → `~220K`. ADD a **PIIBench-vs-PII-Bench disambiguation** note (they are two DISTINCT
  benchmarks — the table's `PII-Bench` row is the 2.8K semantic benchmark; "PIIBench" elsewhere refers to
  a different effort). ADD a one-line sourcing note: "competitor figures as published by their authors
  (as of 2026); verify against primary sources." Leave SPY/PIILO figures as-is unless internally
  inconsistent.

**`TAXONOMY.md`**
- Header (L1/L5) already v2.0.0 / "63". **Prune the body table to EXACTLY the 63** in
  `taxonomy.ENTITY_REGISTRY` (drop the retained-but-non-canonical rows); ensure the per-category counts
  read: contact 2 · digital_online 10 · employment 4 · financial 12 · government_legal 10 ·
  identity_demographics 6 · location_temporal 6 · medical_biological 8 · special_category 5. (Use
  `taxonomy.types_in_category(<cat>)` as the source of truth for each category's rows.)

**`MIGRATION.md`**
- ADD a new top section **"v1.3.0 → v2.0.0"** (above or below the existing v1.0→v1.1 guide) covering: the
  `tier3_evaluation` wrapper consolidating the scattered Tier-3 signal; content-addressed `record_id`
  (+ a `v1_3_0` lineage field); the canonical **63-type** registry (`pii_anon_datasets.taxonomy`);
  back-compat via **`compat.to_v1_record()`**; the **S-PWR enrichment** (159,891→575,604 records;
  ~72% `synthetic_lattice_enrichment`; all 710 committed cells powered; NFR-018 gate); and the pinned
  tags `v1.3.0` + `pre-lattice-enrichment`. Update the doc title to reflect it now covers ≤ v2.0.0.

**`CHANGELOG.md`**
- Under the existing `2.0.0` entry, ADD an **S-PWR sub-entry**: "Statistical-power enrichment (S-PWR):
  corpus 159,891 → **575,604** records (+415,713) / 2,486,438 annotations; ~72%
  `provenance.source_type=synthetic_lattice_enrichment`; all 710 committed lattice cells powered;
  `validate.py --lattice` NFR-018 power gate PASS; tags `v1.3.0` + `pre-lattice-enrichment`." Leave all
  historical version entries unchanged.

**Load-bearing invariants:** (1) **canonical everywhere** — 575,604 / 2,486,438 / 63 / 2.0.0 identical
across the current-state docs; the doc-drift test forbids the stale strings. (2) **caveats surfaced** —
train-vs-eval, ~72% enrichment, §7 power sentence (epistemic honesty). (3) **history preserved** —
historical CHANGELOG entries untouched. (4) **TAXONOMY = registry** — body is exactly the 63
`ENTITY_REGISTRY` types. (5) **/dev-assist-validate → 0 drift**.

## 9. Test type tags

`[PROPERTY-TEST]`

## 10. Definition of Done

- [x] TDD RED→GREEN (6 `nfr_013`-tokened tests in `tests/test_doc_drift.py`; RED `58a4a15` — they fail
      against the current stale docs — precedes the GREEN doc edits `e8dbe77`).
- [x] All 7 docs at canonical 575,604 / 2,486,438 / 63 / 9 / 60 / 7 / v2.0.0; the `159,891 (100%)`
      reframed to the 27.8% Tier-3 substrate; train-vs-eval + ~72%-enrichment + §7-power caveats surfaced.
- [x] MIGRATION has a v1.3.0→v2.0.0 section; CHANGELOG has the S-PWR sub-entry; TAXONOMY body = the 63
      registry types with correct per-category counts; historical CHANGELOG entries preserved.
- [x] full suite 0 regressions (257 → **263 passed**, 1 pre-existing skip); `/dev-assist-validate` → 0
      drift (the NFR-013 measure; `pytest -k nfr_013` → 6 passed).

## 11. Reviewer agents at story gate

code-quality · traceability · requirements-coverage · security-sast · axiom-compliance.
(requirements-coverage + traceability load-bearing: NFR-013 zero-drift across the named docs; the doc-test
pins it. axiom-compliance: the epistemic-honesty caveats (~72% formulaic; synthetic≠external validity) are
surfaced, not hidden.)

## 12. Evidence on completion

**provisional_status: AGENT_SIMULATED.** Executed by `dev-assist-development-executor` in single-tree
mode on branch `feat/v2-scoring-harness`. pytest ran on real local Python 3.10.6 (the doc-drift tests are
real file reads — no simulation needed for them); there is no real CI run attached yet, so Pass 2 (CI on
the merge) is scheduled at the sprint gate. No benchmark hardware is involved (pure doc/string assertions).

### TDD commit SHAs
| Phase | SHA | What |
|---|---|---|
| RED | `58a4a15` | `tests/test_doc_drift.py` — 6 `nfr_013`-tokened `[PROPERTY-TEST]`s; all 6 FAIL against the current stale docs. Status CLAIMED→IN_PROGRESS. |
| GREEN | `e8dbe77` | Remediated all 7 docs to canonical 575,604 / 2,486,438 / 63 / v2.0.0; surfaced the caveats; pruned TAXONOMY to the 63 registry types; added MIGRATION v2 section + CHANGELOG S-PWR sub-entry. 6/6 GREEN. |
| REFACTOR | `6cdf4d0` | Internal-consistency re-read of all 7 docs + ruff/full-suite verification (no further source change; empty commit marking the milestone). |

### pytest RED→GREEN
- **RED** (`58a4a15`): `pytest -k nfr_013` → **6 failed** (each for the right reason: stale record count;
  stale entity count; stale title; non-canonical TAXONOMY rows; missing MIGRATION v2 section; missing
  CHANGELOG S-PWR entry). Full suite at RED: **6 failed, 257 passed, 1 skipped** (the 6 new failures are
  the only failures → no collateral breakage from adding the file).
- **GREEN** (`e8dbe77` / `6cdf4d0`): `pytest -k nfr_013` → **6 passed**; **full suite: 263 passed, 1 skipped**
  (baseline was 257 passed, 1 skipped → **+6 new, 0 regressions**; the 1 skip is pre-existing). `ruff check
  tests/test_doc_drift.py` → All checks passed. `pytest -k nfr_013` correctly selects the token-named tests.

### The 6 tests (all `[PROPERTY-TEST]`, `tests/test_doc_drift.py`)
1. `test_nfr_013_docs_use_canonical_record_count` — README/DATASHEET/COMPARISON contain `575,604` and none
   of `117,752` / `919,000` / `150K+ records` / `~1.24M` / `1.24M`.
2. `test_nfr_013_docs_use_canonical_entity_count` — README/DATASHEET/COMPARISON/`__init__.py` reflect
   `taxonomy.ENTITY_TYPE_COUNT` (== 63, **derived, not hardcoded**) and none of `65 entity types` /
   `65 types` / `57 entity types` / `57 types`.
3. `test_nfr_013_doc_titles_are_v2` — DATASHEET/COMPARISON declare `2.0.0`; their header (first 12 lines)
   carries no `v1.1.0` / `v1.3.0` / `v1.3`.
4. `test_nfr_013_taxonomy_body_matches_registry` — the backticked entity tokens in TAXONOMY.md are EXACTLY
   `taxonomy.CANONICAL_ENTITY_TYPES` (0 extra, 0 missing across all 63).
5. `test_nfr_013_migration_has_v2_section` — MIGRATION.md has a `v1.3.0 → v2.0.0` section mentioning
   `tier3_evaluation`, `record_id`, and `compat.to_v1_record`.
6. `test_nfr_013_changelog_has_spwr_entry` — CHANGELOG.md's `[2.0.0]` section mentions `575,604` and
   `synthetic_lattice_enrichment`.

### Per-file diff summary (8 files; +272 / −77)
- **`src/pii_anon_datasets/__init__.py`** (+4/−4, docstring only): `150K+ records … 65 entity types … 20+
  document formats` → `575,604 records … 63 entity types (canonical: taxonomy.ENTITY_TYPE_COUNT) … 40
  document formats`.
- **`README.md`** (~+43/−…): summary line → 575,604 (159,891 = the Tier-3 eval substrate); Overview
  `159,891`/`~1.24M` → `575,604`/`2,486,438`; the two `159,891 (100%)` rows → `159,891 (27.8% — the Tier-3
  evaluation substrate)`; added the **S-PWR row** + **train-vs-eval note** + **§7 power sentence**
  (verbatim); all `65` entity-count claims → `63`; category table to canonical per-category counts
  (6/2/12/10/10/8/6/4/5); citation block → v2.0.0 / `575,604 records, 63 entity types`. (The one remaining
  `65` is inside the historical phrase "prior 48/65/80 doc drift", which is a record of the reconciliation,
  not a current-state count.)
- **`DATASHEET.md`** (+13/−…): title `v1.1.0` → `v2.0.0`; `117,752 … 919,000+ … 57 entity types` →
  `575,604 … 2,486,438 … 63 entity types`; `(57 types)` → `(63 types; canonical set in
  pii_anon_datasets.taxonomy)`; added the **train-vs-eval + ~72% synthetic_lattice_enrichment** disclosure
  and the **§7 power sentence**; version-history line gains `v2.0.0: May 30, 2026`.
- **`COMPARISON.md`** (~+52/−…): title/headers `v1.3` → `v2.0`; PII-Anon row `160K` → `575,604`, `65` →
  `63`; `All 160K records` → `159,891 Tier-3 records (27.8% eval substrate)` / `159,891 Tier-3 records`;
  four `57 entity types` → `63 entity types`; `150K records` → `575,604 records`; bar charts to v2.0/576K
  (PII-Anon now the largest); competitor deltas **Nemotron 200K→~100K** and **AI4Privacy 580K→~220K**;
  added the **PII-Bench vs "PIIBench" disambiguation** + the **sourcing note**; the stale PII-Anon scale
  multipliers (42x/80x/5x) recomputed against 575,604 (~200x/~440x/~26x — PII-Anon's own number, not an
  invented competitor figure).
- **`TAXONOMY.md`** (+37/−…): body pruned to **exactly the 63** `taxonomy.ENTITY_REGISTRY` types
  (dropped 17 non-canonical rows: DISABILITY_STATUS, FAX_NUMBER, PHONE_COUNTRY_CODE, PHONE_AREA_CODE,
  PHONE_EXTENSION, MOBILE_DEVICE_ID, TRANSACTION_ID, IPV6_ADDRESS, SESSION_ID, COOKIE_ID,
  USER_AGENT_STRING, VEHICLE_REGISTRATION, INSURANCE_CLAIM_NUMBER, GENETIC_MARKER, MEDICAL_DEVICE_UDI,
  BUILDING_NAME, EVENT_DATE; moved `LOCATION_NAME` from identity_demographics → location_temporal); the IPv6
  reference folded into the canonical `IP_ADDRESS` row's example; per-category counts now
  2/10/4/12/10/6/6/8/5; header note updated to "exactly those 63 … enforced by the doc-test".
- **`MIGRATION.md`** (+67/−…): title → `Migration Guide (≤ v2.0.0)`; **added a `v1.3.0 → v2.0.0` section**
  (tier3_evaluation wrapper; content-addressed `record_id` + `v1_3_0_record_id` lineage; canonical 63-type
  registry; `compat.to_v1_record()` back-compat; S-PWR enrichment 159,891→575,604 / ~72%
  `synthetic_lattice_enrichment` / 710 cells powered / NFR-018 gate; `v1.3.0` + `pre-lattice-enrichment`
  tags). The historical **v1.0.0 → v1.1.0** guide (incl. its `117,752`/`57` Statistics-Comparison table) is
  preserved verbatim.
- **`CHANGELOG.md`** (+11): **added an S-PWR sub-entry** under `[2.0.0]` (159,891→575,604 / +415,713 /
  2,486,438 / ~72% `synthetic_lattice_enrichment` / 710 cells / NFR-018 PASS / `v1.3.0` +
  `pre-lattice-enrichment` tags / epistemic note). All historical `[1.3.0]`/`[1.2.0]`/`[1.1.0]`/`[1.0.0]`
  entries (including the 1.3.0 `159,891 (100%)` lines and the 1.2.0 `57 → 65` expansion) are untouched.
- **`tests/test_doc_drift.py`** (NEW, +122): the 6 property tests; entity count derived from
  `taxonomy.ENTITY_TYPE_COUNT`; taxonomy token regex matches all-caps entity-type names (incl. single-word
  AGE/URL/CVV/PIN/IBAN), restricted to backticked occurrences (the type column) so prose isn't over-matched.

### `/dev-assist-validate` 0-drift confirmation
The NFR-013 measure is now machine-enforced: `pytest -k nfr_013` → **6 passed** = 0 doc-drift across the
named current-state docs (canonical 575,604 / 2,486,438 / 63 / 2.0.0 present, all listed stale strings
absent, TAXONOMY body == the registry, MIGRATION v2 section + CHANGELOG S-PWR entry present). A
`/dev-assist-validate` doc-currency check that shells the same suite reports 0 drift. (Real-CI re-run =
Pass 2 at the sprint gate.)

### Deviations & epistemic-honesty notes
- **SPY / PIILO competitor figures left as-is** (8.7K / 22K) — §8b instructs applying ONLY the two
  plan-verified deltas (Nemotron→~100K, AI4Privacy→~220K) and not inventing other competitor numbers; SPY
  and PIILO were internally consistent, so they were not changed.
- **~72%-enrichment caveat surfaced, not hidden** — README/DATASHEET/MIGRATION/CHANGELOG all state the
  enrichment is *formulaic* (raises statistical power) and that **synthetic-distribution power ≠ external
  validity**; the §7 power sentence's "see the real-data correlation slice" pointer is preserved verbatim.
- **History preserved (intentional non-edits)** — the CHANGELOG v1.x entries and the MIGRATION v1.0→v1.1
  Statistics-Comparison table still contain `117,752` / `57` / `159,891 (100%)`; these are records of what
  each prior version claimed and are explicitly excluded from the doc-drift assertions (the negative case in
  §7 exempts historical entries).
- **Out-of-scope finding left untouched** — a pre-existing `mypy --strict [type-arg]` warning on
  `src/pii_anon_datasets/__init__.py:61` (`-> list[dict]`) predates this story (present at baseline
  `703c3e3`) and is on code this DOC story does not own (only the docstring line is owned); per scope
  discipline it was not modified.

### Guardrails (untouched)
Confirmed NO change to: the corpus `data/*.jsonl.gz`, `src/pii_anon_datasets/data/pii_anon.metadata.json`,
`eval_lattice.json`, any git tag, or any scoring/stats code. `git diff` for this story touches exactly the
7 owned docs + `tests/test_doc_drift.py` (8 files; +272/−77).

Story gate **APPROVE** (iteration 1, 5/5 reviewers; `_reviews/story/S5-07/synthesis.md`) — zero
SHOWSTOPPER/CATASTROPHIC/MAJOR. **NFR-013 closed** (documentation-prose arm).

| Reviewer | Verdict | Findings |
|---|---|---|
| code-quality | ✅ APPROVE | 1 OBS (pre-existing `__init__.py:61` type-arg → S5 sprint-close micro-commit) |
| requirements-coverage | ✅ APPROVE | 2 (README:351 MINOR + README:348 OBS — both fixed at close) |
| traceability | ✅ APPROVE | 2 OBS (path shorthand; add NFR-013-verified-by-S5-07 to traceability-matrix at sprint close) |
| axiom-compliance | ✅ APPROVE | 1 OBS (benign historical `65` in a changelog entry) |
| security-sast | ✅ APPROVE | 0 (no real PII/secrets added; doc-test read-only/inert) |

**Joint signals:** zero current-state drift (575,604/2,486,438/63/v2.0.0 identical; no stale strings,
independently re-grepped); M1 single-source via `taxonomy.ENTITY_TYPE_COUNT`; epistemic-honesty caveats
surfaced (~72% formulaic, synthetic≠external-validity, 27.8% train-vs-eval); no fabrication (only the 2
authorized competitor deltas); history preserved (CHANGELOG ADD-ONLY). **Resolved at close:** README:348
("10 benchmarks"→"major competing benchmarks") + README:351 (migration range → v1.0.0→v1.1.0 and
v1.3.0→v2.0.0); doc-drift suite re-run GREEN. **Forwarded to S5 sprint close:** `__init__.py:61` type-arg
micro-fix + the FR-021→S5-06 and NFR-013→S5-07 traceability-matrix rows.
