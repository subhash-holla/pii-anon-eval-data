# S7-05 — Roadmap stub for FR-018/019/020 (documentation only)

> **Cold-pickup**: self-contained. **Test names carry the `fr_018`/`fr_019`/`fr_020` tokens.** A
> `ROADMAP.md` that documents the deferred agentic-leakage items (FR-018 cross-turn fragmented leakage /
> FR-019 transcript-residual estimate / FR-020 live-harness adapter) as **future v1.x roadmap**, honestly
> scoped as NOT implemented in v1. Pinned by `tests/test_roadmap.py`.

## 1. Metadata

| Field | Value |
|---|---|
| **Parent epic** | S7 — Extension seams (DC-10 / DC-14 / DC-01, mostly v1.1) |
| **Sprint** | S-7 (Extension seams) |
| **Status** | DONE |
| **Estimated agent-effort** | XS (≤0.5d) |
| **Owner agent** | orchestrator (doc stub — authored directly, RED→GREEN) |
| **Story gate** | APPROVE at the S7 sprint gate (`_reviews/sprint/S7/synthesis.md`) |
| **provisional_status** | AGENT_SIMULATED |

## 2. Goal

Author `ROADMAP.md` documenting the deferred agentic-leakage requirements as **future v1.x roadmap**,
honestly scoped: FR-018 (cross-turn fragmented-leakage payloads), FR-019 (transcript residual-leakage
estimate with a transcript-distribution caveat), FR-020 (live-harness adapter for AgentDojo / InjecAgent
at AgentLeak's 7 channel boundaries). These are COULD / v1.x roadmap items NOT implemented in v1 — the
roadmap states that plainly (no over-claiming). It also points to the v1 seams that anchor them (the
S7-01 oracle + S7-02 payloads) and the other v1.1/Pass-2 deferrals (FR-015/016 scoring, FR-027 real-data
correlation). Pinned by `tests/test_roadmap.py`.

## 3. Traced requirements

**Primary**: roadmap for FR-018 / FR-019 / FR-020 (COULD / v1.x). **Supporting**: epistemic honesty (these
are documented as future, NOT shipped), FR-017 (the v1 oracle/payload seams these build on).

## 4. Source DC + provisional_status

DC-10 (agentic oracle + payload library — the roadmap items FR-018/019/020 extend it) · AGENT_SIMULATED ·
not DIVERGED. Documentation-only roadmap stub.

## 5. Dependencies

**Must be DONE**: none. **Blocks**: S7 sprint close (the roadmap closes the FR-018/019/020 documentation).

## 6. Test-first plan (RED→GREEN) — `fr_018`/`fr_019`/`fr_020` tokened

`tests/test_roadmap.py`:
1. `test_fr_018_fr_019_fr_020_roadmap_documents_deferred_items` — `ROADMAP.md` exists and names FR-018,
   FR-019, FR-020 and describes each (cross-turn fragmented leakage / transcript-residual / live-harness
   adapter) — `[PROPERTY-TEST]`.
2. `test_fr_020_roadmap_frames_items_as_future_not_shipped` — `ROADMAP.md` frames these as roadmap /
   future / v1.x (not implemented in v1) — honest scoping, no over-claim; it references the v1 seams
   (oracle / payloads) they build on — `[PROPERTY-TEST]`.

## 7. Acceptance criteria

> **Given** the deferred agentic-leakage requirements, **When** ROADMAP.md is published, **Then** FR-018,
> FR-019, FR-020 are documented as future v1.x items (not shipped in v1), anchored to the v1 seams.
> **Negative**: the roadmap does NOT claim these are implemented.

## 8. Files this story owns

**NEW**: `ROADMAP.md`, `tests/test_roadmap.py`

## 8b. Implementation spec (authoritative — required ROADMAP.md content)

ROADMAP.md MUST: name FR-018 / FR-019 / FR-020 with a one-line description each; flag them as **v1.x
roadmap / NOT implemented in v1**; point to the v1 seams (the S7-01 PII-recognition oracle + S7-02
injection payload library) that anchor the agentic-leakage direction; and list the v1.1/Pass-2 deferrals
(FR-015/016 coreference/quasi-id scoring; FR-027 real-data correlation — the harness ships, the real-data
correlation is Pass-2). Honest framing throughout (no over-claiming; "never marketed as agent-leakage
scoring" stays true).

**Load-bearing invariants:** (1) FR-018/019/020 named + described + flagged future/not-shipped. (2)
honesty — no claim that the roadmap items are implemented.

## 9. Test type tags

`[PROPERTY-TEST]`

## 10. Definition of Done

- [x] RED→GREEN (2 `fr_018`/`fr_019`/`fr_020`-tokened tests; RED — ROADMAP.md absent — precedes GREEN).
- [x] ROADMAP.md documents FR-018/019/020 as future v1.x (not shipped), anchored to the v1 seams.
- [x] full suite 0 regressions.

## 11. Reviewer agents at story gate

Reviewed at the **S7 sprint gate** (requirements-coverage confirms FR-018/019/020 are roadmap-documented;
axiom-compliance confirms the honest "not shipped" framing). A doc stub does not need a separate
5-reviewer story gate.

## 12. Evidence on completion

**provisional_status: AGENT_SIMULATED.** Authored directly (orchestrator, RED→GREEN). **TDD:** RED
`f137aa7`-successor (`tests/test_roadmap.py` — 2 `fr_018`/`fr_019`/`fr_020` tests fail, ROADMAP.md absent)
→ GREEN (`ROADMAP.md`). 2/2 GREEN; full suite **346 passed / 1 skipped** (344 prior + 2). **The 2 tests:**
`test_fr_018_fr_019_fr_020_roadmap_documents_deferred_items` (names + describes FR-018/019/020),
`test_fr_020_roadmap_frames_items_as_future_not_shipped` (roadmap/future framing + oracle/payload anchor).
**ROADMAP.md audit:** FR-018 (cross-turn fragmented leakage), FR-019 (transcript-residual), FR-020
(live-harness adapter — AgentDojo/InjecAgent, AgentLeak 7 boundaries) all flagged **roadmap / NOT
implemented in v1**; the v1.1/Pass-2 deferrals (FR-015/016 scoring, FR-027 real-data correlation) listed;
honesty note ("nothing here is implemented in v1"; "never agent-leakage scoring"; synthetic ≠ external
validity). Reviewed at the S7 sprint gate (requirements-coverage + axiom-compliance).
