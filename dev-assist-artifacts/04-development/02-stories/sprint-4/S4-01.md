# S4-01 — Clopper-Pearson exact interval (stdlib incomplete-beta inverse)

> **Cold-pickup**: self-contained; §8b authoritative. **Test names carry `fr_004`/`nfr_002` tokens.**
> Pure-stdlib (NFR-004) — the inverse regularized incomplete beta is hand-rolled (no scipy/numpy),
> reusing the `Interval` dataclass + the reidx-02 integer guard from S1's `wilson_interval`.

## 1. Metadata

| Field | Value |
|---|---|
| **Parent epic** | S4 — Stats/reporting completion (DC-09) |
| **Sprint** | S-4 (Stats/reporting) |
| **Status** | DONE |
| **Estimated agent-effort** | M (≤3d) |
| **Owner agent** | dev-assist-development-executor (claimed 2026-05-29; IN_PROGRESS → REVIEW 2026-05-29) |
| **Story gate** | APPROVE (iter 1, 5/5 reviewers; `_reviews/story/S4-01/synthesis.md`) |
| **provisional_status** | AGENT_SIMULATED |

## 2. Goal

Add the **exact Clopper-Pearson** binomial confidence interval as a second CI method alongside Wilson
(FR-004 offers both; NFR-002 requires every CI carry a NAMED method). Implemented pure-stdlib via the
inverse regularized incomplete beta function (Beta quantiles), integer-guarded identically to
`wilson_interval` (reidx-02). Clopper-Pearson is the conservative exact interval used where guaranteed
coverage matters (e.g. small-n credential cells).

## 3. Traced requirements

**Primary**: FR-004 (CI on every metric — Clopper-Pearson option), NFR-002 (CI method named; zero bare point estimates).
**Supporting**: AX-003 (statistical rigor), NFR-004 (pure-stdlib), reidx-02 (integer-count guard).

## 4. Source DC + provisional_status

DC-09 · AGENT_SIMULATED · not DIVERGED.

## 5. Dependencies

**Must be DONE**: S1 (`stats/intervals.py` `Interval` + `wilson_interval` + `_z`). ✅
**Blocks**: S4-04 (per-language power table may offer CP), Stage 5 NFR verification.

## 6. Test-first plan (RED→GREEN) — `fr_004`/`nfr_002` tokened

`tests/test_clopper_pearson.py`:
1. `test_fr_004_clopper_pearson_textbook_values` — at 95%: `clopper_pearson_interval(0,10)` ≈ [0.0, 0.3085]; `(1,10)` ≈ [0.0025, 0.4450]; `(10,10)` ≈ [0.6915, 1.0] — to 4 d.p. (cross-checks the inverse-beta against a known table) — `[UNIT-TEST]`.
2. `test_fr_004_clopper_pearson_brackets_point` — `low <= k/n <= high` for assorted (k,n); point == k/n — `[UNIT-TEST]`.
3. `test_nfr_002_clopper_pearson_method_named` — `method == "clopper-pearson"`; `as_dict()` carries it — `[UNIT-TEST]`.
4. `test_fr_004_reidx02_clopper_pearson_integer_guard` — fractional/`bool` k or n → `TypeError` (same guard as Wilson) — `[UNIT-TEST]`.
5. `test_fr_004_clopper_pearson_edges` — `n=0` → the n=0 sentinel (`point=nan, low=0, high=1, n=0`); `k=0` → low exactly 0.0; `k=n` → high exactly 1.0 — `[UNIT-TEST]`.
6. `test_fr_004_clopper_pearson_is_conservative_vs_wilson` — for the same (k,n) with `0<k<n`, the CP interval CONTAINS the Wilson interval (CP wider/conservative) — `[PROPERTY-TEST]`.
7. `test_fr_004_clopper_pearson_deterministic` — fixed iteration counts → byte-identical across runs (NFR-004) — `[PROPERTY-TEST]`.

## 7. Acceptance criteria (G/W/T from FR-004 / NFR-002)

> **Given** integer success/trial counts (k, n),
> **When** `clopper_pearson_interval(k, n, confidence)` is computed,
> **Then** an exact Clopper-Pearson `Interval` is returned with `method == "clopper-pearson"`, bounded by the Beta quantiles, integer-guarded, and at least as conservative as Wilson.
> **Negative**: fractional/bool counts → `TypeError`; bare point estimates are impossible (the function returns an `Interval`, never a float).

## 8. Files this story owns

**MODIFIED**: `src/pii_anon_datasets/stats/intervals.py` (ADD `clopper_pearson_interval` + private `_betacf`/`_betainc`/`_inv_betainc`; PRESERVE `Interval`/`wilson_interval`/`_z` exactly), `src/pii_anon_datasets/stats/__init__.py` (export `clopper_pearson_interval`).
**NEW**: `tests/test_clopper_pearson.py`.

## 8b. Implementation spec (authoritative)

Pure-stdlib (`math` only). Same integer guard + `Interval` shape as `wilson_interval`.

```python
def clopper_pearson_interval(k: int, n: int, confidence: float = 0.95) -> Interval:
    # integer guard IDENTICAL to wilson_interval (reidx-02): reject non-int / bool k,n; require 0<=k<=n
    # n == 0 → return the same n=0 sentinel Interval(nan, 0.0, 1.0, 0, "clopper-pearson", confidence)
    # alpha = 1 - confidence
    # low  = 0.0      if k == 0 else _inv_betainc(alpha/2,       k,   n - k + 1)
    # high = 1.0      if k == n else _inv_betainc(1 - alpha/2,   k+1, n - k)
    # point = k / n
    # return Interval(point, low, high, n, "clopper-pearson", confidence)
```

**Inverse regularized incomplete beta** (no scipy):
- `_betacf(a, b, x)` — Lentz continued fraction for `I_x(a,b)` (Numerical Recipes §6.4); fixed max-iter (~200) + 1e-12 tol.
- `_betainc(a, b, x)` — `exp(lgamma(a+b) - lgamma(a) - lgamma(b) + a*ln x + b*ln(1-x)) * _betacf(...)/a`, with the symmetry swap `x > (a+1)/(a+b+2)` → `1 - _betainc(b, a, 1-x)`. `math.lgamma` is stdlib.
- `_inv_betainc(p, a, b)` — **bisection** on `[0,1]` (monotone in x; ~60 iters → 1e-12). Bisection (not Newton) is dependency-free + robust at the k=0/k=n edges. Fixed iteration count → deterministic (NFR-004).

Reuse `Interval` + the integer-guard helper from `intervals.py` (factor the guard out of `wilson_interval` if it isn't already, WITHOUT changing Wilson's behavior).

## 9. Test type tags

`[UNIT-TEST]` `[PROPERTY-TEST]`

## 10. Definition of Done

- [x] TDD RED→GREEN→REFACTOR (7 `fr_004`/`nfr_002`-tokened tests; RED precedes GREEN). — RED `8939c46`, GREEN `acac5a8`, REFACTOR `c4dc62c`.
- [x] Textbook cross-check (0/10, 1/10, 10/10 @95%) to 4 d.p.; conservative-vs-Wilson property holds. — see §12.
- [x] Integer-guarded (reidx-02); n=0 sentinel; k=0 → low 0.0, k=n → high 1.0.
- [x] `method == "clopper-pearson"` (NFR-002); deterministic (fixed iterations; byte-identical in-process + cross-process).
- [x] `Interval`/`wilson_interval` preserved (Wilson 6/6 byte-identical); `ruff` clean + `mypy --strict` clean on added lines (lone residual = pre-existing S1 `as_dict -> dict`); full suite 0 regressions (176 passed, 1 skipped).

## 11. Reviewer agents at story gate

code-quality · traceability · requirements-coverage · security-sast · axiom-compliance.

## 12. Evidence on completion

> **provisional_status: AGENT_SIMULATED.** Tests executed locally under the agent
> (Python 3.10.6, pytest 9.0.2, `pythonpath=src`) — not a real CI runner. Pass 2 (real
> CI on hardware) remains to be scheduled before the AGENT_SIMULATED tag is cleared.

**TDD commits (branch `feat/v2-scoring-harness`):**

| Phase | SHA | Note |
|---|---|---|
| RED | `8939c46` | `tests/test_clopper_pearson.py` — 7 `fr_004`/`nfr_002`-tokened tests; collection ImportError (`clopper_pearson_interval` absent) → genuine RED. |
| GREEN | `acac5a8` | ADD `clopper_pearson_interval` + `_betacf`/`_betainc`/`_inv_betainc` + shared `_require_int_counts`; export from `stats/__init__.py`. |
| REFACTOR | `c4dc62c` | `ruff --fix` import-order on the new test (I001); no logic change. |

**RED → GREEN:** RED = 7 failing (ImportError at collection, function not implemented); GREEN = **7 passed** (`7 passed in 0.17s`). RED strictly precedes GREEN.

**Full suite (0 regressions):** `176 passed, 1 skipped in ~3.3s` = 169 prior + 7 new; the 1 skip is pre-existing. `test_intervals.py` (Wilson) **6/6 byte-identical** after the integer-guard was factored into the shared `_require_int_counts` (Wilson behavior preserved; no `match=` on message text in its tests).

**Textbook cross-check @95% (4 d.p.) — validates the hand-rolled inverse-beta:**

| (k, n) | low | high | expected |
|---|---|---|---|
| (0, 10) | 0.0000 | 0.3085 | [0.0, 0.3085] ✓ |
| (1, 10) | 0.0025 | 0.4450 | [0.0025, 0.4450] ✓ |
| (10, 10) | 0.6915 | 1.0000 | [0.6915, 1.0] ✓ |

**FR-004 conservatism (CP ⊇ Wilson):** verified on (1,10),(3,10),(5,10),(13,20),(49,100) — `cp.low ≤ wilson.low` and `cp.high ≥ wilson.high` on every cell (correctness check on the inverse-beta passes).

**Edges:** n=0 → sentinel `(point=nan, low=0.0, high=1.0, n=0, "clopper-pearson")`; k=0 → low exactly `0.0`; k=n → high exactly `1.0`.

**NFR-002:** `method == "clopper-pearson"`, surfaced via `as_dict()`. **reidx-02:** fractional/bool k or n → `TypeError`; out-of-range → `ValueError`. **NFR-004:** pure-stdlib (`math` only — no numpy/scipy); `_inv_betainc` bisection fixed at 60 iters, `_betacf` ≤200 — byte-identical bounds across 5 in-process calls **and** across two independent Python processes.

**Lint:** `ruff check` on `intervals.py`, `stats/__init__.py`, and the test → **all clean**. `mypy --strict --follow-imports=silent intervals.py` → **clean on all S4-01-added lines**; one residual error is the **pre-existing S1** annotation `Interval.as_dict(self) -> dict` (line 28), which §8 instructs me to PRESERVE exactly — confirmed identical on the pre-S4-01 file, so out of this story's owned-change scope (flag for a future S1-scoped typing fix).

**Reviewer verdicts:** _(pending story-gate dispatch: code-quality · traceability · requirements-coverage · security-sast · axiom-compliance)._
