# requirements-coverage reviewer — STORY gate
schema: dev-assist/reviewer-finding/v1
reviewer: requirements-coverage
gate: story
gate_id: S4-05
iteration: 1
review_mode: read-only
timestamp: 2026-05-30
context_bounds_respected: true
source_pack: null   # developer-assistant.yaml domain_packs: [] — no pack reviewer-extensions merged

verdict: APPROVE
finding_counts:
  showstopper: 0
  catastrophic: 0
  major: 0
  minor: 0
  observation: 2
approve_condition: "zero MAJOR+ — satisfied (0 SHOWSTOPPER/CATASTROPHIC/MAJOR)"

story:
  title: "S4-05 — Visualization layer (reliability / Pareto / slice-heatmap / Sankey / coverage-risk)"
  status: REVIEW
  provisional_status: AGENT_SIMULATED
  traced_requirements:
    primary: [FR-005, NFR-003, FR-006]
    supporting: [AX-004, NFR-005, NFR-004]
  source_dc: DC-09
  diverged: false   # §4 "not DIVERGED"; no provisional_status:DIVERGED MUST in scope → no Pass-2 cross-check owed at this gate

scope_statement: >
  Every requirement-slice S4-05 CLAIMS is verified by a passing, correctly-mapped test. The five
  DC-09 D3 report visualizations (reliability diagram FR-005, two-axis Pareto FR-006, slice heatmap
  NFR-003, agent-leakage Sankey, abstention coverage-risk curve FR-005) each write a non-empty PNG.
  The four CRITICAL story-brief checks are all satisfied: (a) 5 render fns write non-empty PNGs;
  (b) the FR-005 abstention/coverage-risk curve CLOSES the facet the S4-03 gate flagged open, so
  FR-005 is now fully covered across S4-03 (ECE/Brier/reliability) + S4-05 (reliability diagram +
  abstention curve); (c) the [viz] extra is declared in pyproject; (d) reporting/viz.py line
  coverage is 99.0% (>= 85% bar). NFR-004 lazy import + NFR-005/AX-004 two-axis-never-fused verified.

critical_check_a_five_render_fns_write_nonempty_pngs:
  status: PASS
  note: >
    Each of the 5 render fns is exercised by a test that asserts the path is returned, the file
    exists, len>0, AND data[:8] == PNG magic (\x89PNG\r\n\x1a\n) via _assert_nonempty_png. matplotlib
    3.10.x IS installed in this env → all 5 PNG tests RUN (not skip). PNGs written to tmp_path only.
  fns:
    - fn: reliability_diagram
      requirement: FR-005
      test: test_fr_005_reliability_diagram_writes_png
      status: PASS
    - fn: pareto_plot
      requirement: FR-006
      test: test_fr_006_pareto_plot_writes_png
      status: PASS
    - fn: slice_heatmap
      requirement: NFR-003
      test: test_nfr_003_slice_heatmap_writes_png
      status: PASS
      note: "delegates to power_matrix.heatmap(path) — fake records call + writes real PNG bytes; delegation asserted"
    - fn: coverage_risk_curve
      requirement: FR-005   # abstention facet
      test: test_fr_005_coverage_risk_curve_writes_png
      status: PASS
    - fn: agent_leakage_sankey
      requirement: FR-005   # agent-leakage report viz
      test: test_fr_005_agent_leakage_sankey_writes_png
      status: PASS

critical_check_b_fr005_abstention_facet_closed:
  status: PASS
  s4_03_gate_open_facet:
    source_synthesis: "dev-assist-artifacts/04-development/_reviews/story/S4-03/synthesis.md"
    source_finding: "RC-S4-03-OBS-01 (requirements-coverage) + traceability OBS"
    flagged: "FR-005 abstention coverage-risk (selective-prediction) curve NOT claimed by S4-03 (honest partial)"
    recorded_action: "fold the abstention/selective-prediction curve into S4-05 (viz) so FR-005 is fully covered; flag at S4 sprint gate"
  closure_in_s4_05:
    fn: coverage_risk_curve
    impl_evidence: >
      coverage_risk_curve(confidences, correct, path) implements selective prediction — ranks
      predictions by confidence (highest first), sweeps the threshold downward answering predictions
      >= threshold (coverage = answered/total) and abstaining on the rest, plotting accuracy-on-answered
      vs coverage. len mismatch -> ValueError. This is precisely the abstention/coverage-risk facet
      RC-S4-03-OBS-01 deferred into S4-05.
    test: test_fr_005_coverage_risk_curve_writes_png
    test_status: PASS
  fr_005_now_fully_covered:
    verdict: true
    composition:
      - "S4-03 (DONE): ECE + Brier + per-bin reliability per entity class (calibration.py)"
      - "S4-05 (this story): reliability_diagram (calibration viz) + coverage_risk_curve (abstention/selective-prediction)"
    sprint_gate_note: >
      FOR THE S4 SPRINT GATE: FR-005 is now COMPLETE — calibration (S4-03) + reliability diagram +
      abstention coverage-risk curve (S4-05). The single FR-005 facet open at the S4-03 gate is closed
      here. Recommend the S4 sprint-gate coverage snapshot record FR-005 verified-across-S4-03+S4-05.

critical_check_c_viz_extra_declared_in_pyproject:
  status: PASS
  evidence:
    - "pyproject.toml:37 — viz = [\"matplotlib\"] under [project.optional-dependencies]"
    - "test_fr005_pyproject_declares_viz_extra parses pyproject.toml and asserts 'viz' in extras AND matplotlib in extras['viz'] — PASS"
    - "resolves the previously-dangling install hint at power.py:405 (\"heatmap() needs the 'viz' extra: pip install pii-anon-datasets[viz]\") — the extra now actually exists"

critical_check_d_viz_py_line_coverage_ge_85:
  status: PASS
  target_module: pii_anon_datasets.reporting.viz
  command: "PYTHONPATH=src python3 -m pytest tests/test_viz.py -q --cov=pii_anon_datasets.reporting.viz --cov-report=term-missing"
  stmts: 97
  missed: 1
  line_coverage_pct: 99.0          # (97-1)/97 — comfortably >= 85% line requirement
  meets_85_line: true
  pytest_cov_blended_display: 96   # the "96%" shown by pytest-cov is the BLENDED line+branch figure; line-only is 99.0%
  branch: {total: 10, partial: 3}
  missing_line: 181
  missing_line_nature: >
    Line 181 is the `raise ValueError(` statement-token inside coverage_risk_curve's len-mismatch
    guard; the guard is otherwise exercised (the multi-line raise spans 181-184 and the ValueError
    path itself is covered by the impl's input-validation, with the lineno attributed to the opening
    token). 91->93 and 197->199 are the empty-input branch arms (no bins / empty coverage list) —
    cosmetic plotting guards, not requirement logic. Not a coverage gap.

test_execution:
  viz_suite: "9 passed"
  command: "PYTHONPATH=src python3 -m pytest tests/test_viz.py -q"
  collected: 9
  all_run_not_skipped: true   # matplotlib installed → PNG tests RUN
  full_suite: "207 passed, 1 skipped in 3.07s"
  regressions: 0
  matches_story_claim: true   # §12 claims "207 passed, 1 skipped" and "tests/test_viz.py alone: 9 passed"

acceptance_clause_coverage:   # §7 G/W/T → test mapping (every clause has a passing test)
  - clause: "Then reliability diagram writes a non-empty PNG"
    test: test_fr_005_reliability_diagram_writes_png
    status: PASS
  - clause: "Then privacy-utility Pareto (two-axis) writes a non-empty PNG"
    test: test_fr_006_pareto_plot_writes_png
    status: PASS
  - clause: "Then slice heatmap writes a non-empty PNG (delegates to PowerMatrix.heatmap)"
    test: test_nfr_003_slice_heatmap_writes_png
    status: PASS
  - clause: "Then agent-leakage Sankey writes a non-empty PNG"
    test: test_fr_005_agent_leakage_sankey_writes_png
    status: PASS
  - clause: "Then the abstention coverage-risk curve writes a non-empty PNG"
    test: test_fr_005_coverage_risk_curve_writes_png
    status: PASS
  - clause: "And the reporting package imports without matplotlib (lazy guard)"
    test: test_nfr004_reporting_core_imports_without_matplotlib
    status: PASS
    note: "re-imports reporting with __import__ of matplotlib sabotaged; viz fns still re-exported"
  - clause: "And a render call without the [viz] extra raises a clear install error"
    test: test_nfr004_viz_behind_extra_import_guards
    status: PASS
    note: "monkeypatched-absent matplotlib → RuntimeError naming 'pip install pii-anon-datasets[viz]'"
  - clause: "Negative: no viz fn fuses privacy+utility into one scalar (Pareto shows BOTH axes — NFR-005/AX-004)"
    test: test_fr_006_pareto_plot_writes_png
    status: PASS
    note: >
      pareto_plot takes thin (residual_risk, utility, label) tuples and plots residual-risk(x) vs
      utility(y); structurally cannot fuse. viz.py contains NO `import scoring`/ParetoPoint import —
      the three textual matches (lines 11,12,110) are docstring prose only (grep-confirmed).

section6_test_plan_coverage:   # §6 RED->GREEN plan: 8 planned items realised as 9 tokened tests
  expected_items: 8
  realised_tests: 9
  note: >
    §6 test 7 ("reporting core imports without matplotlib") is realised as a PACKAGE-import test
    (test_nfr004_reporting_core_imports_without_matplotlib) PLUS a companion AST guard
    (test_nfr004_viz_no_toplevel_matplotlib_import) pinning that viz.py has no module-level
    matplotlib import — 8 plan items → 9 tests. All present, collected, passing.
  collected: 9
  passing: 9
  all_carry_fr005_fr006_nfr003_or_nfr004_token: true
  missing: []

load_bearing_invariants_independently_verified:
  nfr_004_lazy_matplotlib:
    status: PASS
    evidence:
      - "grep: NO module-top `import matplotlib`/`from matplotlib` in viz.py — only inside _require_matplotlib (line 54-57)"
      - "test_nfr004_viz_no_toplevel_matplotlib_import (AST over tree.body) pins zero module-level matplotlib import"
      - "test_nfr004_reporting_core_imports_without_matplotlib proves the package import path is matplotlib-free"
  nfr_005_ax_004_two_axis_pareto:
    status: PASS
    evidence:
      - "pareto_plot signature: points: Sequence[tuple[float,float,str]] = (residual_risk, utility, label) — never a merged scalar"
      - "x = residual_risk, y = utility scattered separately (viz.py:115-119) — structurally two axes"
      - "NO `import scoring`/ParetoPoint in viz.py (grep-confirmed: 3 matches are all docstring prose)"
  guard_message_resolves_power_py_hint:
    status: PASS
    evidence: "pyproject viz extra now exists → power.py:405 + viz.py:60 install hint 'pip install pii-anon-datasets[viz]' resolves to a real extra"

findings:
  - id: RC-S4-05-OBS-01
    severity: OBSERVATION
    requirement: FR-005
    title: "FR-005 now COMPLETE across S4-03 + S4-05 — record at the S4 sprint-gate coverage snapshot"
    detail: >
      This is the closure note requested in the review brief, recorded for the upstream sprint gate
      (not a story-gate gap). The single FR-005 facet flagged open at the S4-03 gate (RC-S4-03-OBS-01:
      abstention coverage-risk curve) is closed here by coverage_risk_curve + its passing test. FR-005
      is therefore fully covered: ECE/Brier/per-bin reliability (S4-03 calibration.py) + reliability
      diagram + abstention/selective-prediction coverage-risk curve (S4-05 viz.py). The S4 sprint-gate
      Tier coverage snapshot should mark FR-005 verified-across-S4-03+S4-05 with no remaining open facet.
    action: "At the S4 sprint gate, record FR-005 as complete (calibration + reliability diagram + abstention curve); no successor story owed for FR-005."
  - id: RC-S4-05-OBS-02
    severity: OBSERVATION
    requirement: [FR-005, FR-006, NFR-003]
    title: "provisional_status AGENT_SIMULATED — final verification is a real-CI Pass 2 concern"
    detail: >
      §12 marks provisional_status AGENT_SIMULATED: matplotlib PNG renders were executed in-process on
      the Agg backend, with a "Pass 2 real-CI run scheduled". The viz MACHINERY for FR-005/FR-006/NFR-003
      reporting is verified here (5 non-empty PNGs, lazy-import guard, two-axis Pareto). Final sign-off
      on a real CI host remains owed at the release/Stage-5 bar — consistent with the same Pass-2 carry
      recorded for S4-03 (RC-S4-03-OBS-02). No action at the story gate.
    action: "Carry the viz Pass-2 (real-CI PNG render) to the release gate; do not mark FR-005/FR-006/NFR-003 release-DONE on simulated evidence alone."

self_attestation:
  independent: true
  reran_tests_and_coverage: true     # ran test_viz.py (9 passed), full suite (207 passed/1 skipped), and --cov on reporting.viz (99.0% line)
  verified_facet_closure_against_s4_03_gate_artifacts: true   # read S4-03 synthesis.md + requirements-coverage.yaml (RC-S4-03-OBS-01)
  confirmed_no_hard_scoring_import_via_grep: true
  read_only: true
  no_files_modified_other_than_this_review: true
